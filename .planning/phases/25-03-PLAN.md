# Plan 25-03 — Deploy, Rebuild, Verify

## Tasks

1. Sync code to VPS
2. `docker compose up -d --build api web worker`
3. Run `bash scripts/smoke-test-all.sh`
4. Run `bash scripts/perf-check.sh`
5. Update `.planning/STATE.md` with results

## Rollback

```bash
docker compose up -d  # previous images if build fails
# Restore from /home/ubuntu/backups/kidspot_YYYYMMDD_0400.dump if needed
```
