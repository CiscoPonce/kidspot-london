# Phase 07.5-04 - Test Coverage Expansion

## 1. Objective
Expand Vitest coverage to 40-50% focusing on the new Yelp service, core routes, and scoring mechanisms.

## 2. Requirements
- **TEST-02**: Expand test coverage to at least 40-50%.

## 3. Tasks
1. **Write Unit Tests**: Add tests for the new Yelp waterfall logic in `backend/src/tests/venueService.test.ts`.
2. **Write Integration Tests**: Add integration tests for search endpoints and cache behavior.
3. **Write Worker Tests**: Add unit tests for `worker.ts` jobs.

## 4. Dependencies
- 07.5-01-PLAN.md
- 07.5-03-PLAN.md

## 5. Verification
- `npm run test -- --coverage` reports > 40% statement coverage.
- All tests pass in the CI pipeline.