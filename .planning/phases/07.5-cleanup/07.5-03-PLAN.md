# Phase 07.5-03 - Yelp Fusion API Waterfall

## 1. Objective
Replace Google Places API with Yelp Fusion API in `venueService.ts` and discovery scripts. Implement fallback logic (OSM -> Yelp -> Redis cache).

## 2. Requirements
- **API-04**: Replace Google Places API with Yelp Fusion waterfall approach.

## 3. Tasks
1. **Implement Waterfall Logic**: Update `backend/src/services/venueService.ts` to check the DB first, then ping Yelp Fusion, and save the merged payload in Redis for 24 hours.
2. **Update Discovery Scripts**: Refactor `backend/scripts/discovery/google-places-discovery.js` to `yelp-fusion-discovery.ts` (or update existing integration).
3. **Remove Google Places**: Strip out Google Places SDK, API keys, and FieldMask logic.

## 4. Dependencies
- 07.5-01-PLAN.md (Worker TS migration)

## 5. Verification
- Yelp API fetches correctly using a valid key.
- Results are cached in Redis.
- Zero-cost architecture is maintained.