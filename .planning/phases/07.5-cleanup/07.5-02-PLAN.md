# Phase 07.5-02 - Phase 8 Database Migrations

## 1. Objective
Create database schema migrations to prepare for the Phase 8 revenue loop, including Stripe sponsorship tiers, claim statuses, and Yelp Fusion cache columns if needed.

## 2. Requirements
- **DB-01**: Create database migrations for Phase 8 features.

## 3. Tasks
1. **Create Migration Script**: Create `backend/db/migrations/006_revenue_schema.sql` adding:
   - `claim_status` (unclaimed, pending, verified)
   - `stripe_customer_id`, `stripe_subscription_id`
   - `sponsorship_tier` (none, premium, featured)
2. **Apply Migration**: Ensure the migration runs smoothly on top of the existing schema.

## 4. Dependencies
- None.

## 5. Verification
- Migration script runs successfully in PostgreSQL.
- Database schema matches the new structure without dropping existing data.