# Plan 25-02 — Wave B Discovery Sweep

## Tasks

1. Run Google Places discovery for under-represented boroughs
2. Re-classify new venues (`classify-venue-scope.sql`)
3. Log before/after core venue counts

## Command

```bash
cd /home/ubuntu/kidspot && source .env
export DATABASE_URL="postgres://kidspot_admin:${DB_PASSWORD}@127.0.0.1:5432/kidspot"
cd backend && npx tsx scripts/discovery/sources/google-places-discovery.ts 30
```

Or use the wrapper:

```bash
bash scripts/run-wave-b-discovery.sh
```

## Success

Discovery script completes without fatal error; core venue count increases or script reports full coverage.
