---
phase: 02-continuous-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - backend/scripts/cron-agent.js
  - backend/db/schema.sql
autonomous: true
requirements:
  - DISCOVER-03
user_setup: []
must_haves:
  truths:
    - "Stale venues are detected based on last_scraped timestamp"
    - "Stale threshold is configurable (default 24 hours)"
    - "Cron agent processes stale venues in priority order"
    - "Sponsored venues are prioritized for updates"
    - "Stale venue detection is efficient (uses database index)"
  artifacts:
    - path: "backend/scripts/cron-agent.js"
      provides: "Cron agent with stale venue detection"
      contains: "get_venues_needing_scrape(), processStaleVenues()"
    - path: "backend/db/schema.sql"
      provides: "Database schema with last_scraped field and index"
      contains: "last_scraped TIMESTAMPTZ, idx_venues_last_scraped"
  key_links:
    - from: "backend/scripts/cron-agent.js"
      to: "get_venues_needing_scrape function"
      via: "SQL query"
      pattern: "SELECT * FROM get_venues_needing_scrape"
    - from: "backend/db/schema.sql"
      to: "last_scraped index"
      via: "CREATE INDEX"
      pattern: "CREATE INDEX idx_venues_last_scraped"
---

<objective>
Implement stale venue detection to identify venues that need updating based on last_scraped timestamp.

Purpose: Ensure data freshness by detecting venues that haven't been updated recently and prioritizing them for refresh. Sponsored venues are prioritized to ensure their data stays fresh.

Output: Working stale venue detection that efficiently identifies venues needing updates and prioritizes sponsored venues.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/02-continuous-discovery/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement get_venues_needing_scrape function</name>
  <files>backend/db/schema.sql</files>
  <action>Update backend/db/schema.sql to add get_venues_needing_scrape function:
1. Function takes stale_hours parameter (default 24)
2. Returns venues where last_scraped is NULL or older than threshold
3. Orders results by:
   - Sponsor tier (gold > silver > bronze > none)
   - last_scraped timestamp (oldest first)
4. Limits results to 100 venues per batch
5. Returns: id, name, source, source_id, last_scraped

This function efficiently identifies venues needing updates using the last_scraped index.</action>
  <verify>get_venues_needing_scrape function returns correct venues</verify>
  <done>get_venues_needing_scrape function implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement processStaleVenues function</name>
  <files>backend/scripts/cron-agent.js</files>
  <action>Update backend/scripts/cron-agent.js to add processStaleVenues function:
1. Calls get_venues_needing_scrape with stale threshold
2. Processes venues in batches
3. For each venue:
   - Calls updateVenue function to refresh data
   - Logs progress (processed, updated, deactivated, errors)
   - Adds delay between requests to respect rate limits
4. Reports summary statistics

This function processes all stale venues and updates their information.</action>
  <verify>processStaleVenues function processes all stale venues</verify>
  <done>processStaleVenues function implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add last_scraped index</name>
  <files>backend/db/schema.sql</files>
  <action>Update backend/db/schema.sql to add last_scraped index:
1. Create index on last_scraped column
2. Index supports efficient stale venue detection
3. Index is used by get_venues_needing_scrape function

This index ensures efficient stale venue detection even with millions of venues.</action>
  <verify>last_scraped index is created and used by queries</verify>
  <done>last_scraped index added</done>
</task>

<task type="auto">
  <name>Task 4: Test stale venue detection</name>
  <files>backend/scripts/test-stale-detection.js</files>
  <action>Create backend/scripts/test-stale-detection.js that:
1. Inserts test venues with different last_scraped timestamps
2. Runs get_venues_needing_scrape with various thresholds
3. Verifies that correct venues are returned
4. Verifies that sponsored venues are prioritized
5. Verifies that index is used (check query plan)
6. Reports test results

This test script validates that stale venue detection works correctly and efficiently.</action>
  <verify>node backend/scripts/test-stale-detection.js passes all tests</verify>
  <done>Stale venue detection tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stale Detection → Database | Query for venues needing updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | Stale Detection Query | mitigate | Use index on last_scraped, LIMIT results to 100 |
| T-02-03-02 | Information Disclosure | Stale Venue Data | accept | Venue data is public information |

**Critical Mitigations for This Plan:**
- Use index on last_scraped for efficient queries
- Limit results to prevent large result sets
- Prioritize sponsored venues for updates
- Batch processing to prevent overwhelming APIs
</threat_model>

<verification>
- get_venues_needing_scrape function is implemented
- processStaleVenues function is implemented
- last_scraped index is created
- Stale venues are detected correctly
- Sponsored venues are prioritized
- Index is used by queries
- Test script validates all functionality
</verification>

<success_criteria>
- get_venues_needing_scrape function is complete
- processStaleVenues function is complete
- last_scraped index is created
- Stale venues are detected correctly
- Sponsored venues are prioritized
- Index is used for efficient queries
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-continuous-discovery/02-03-SUMMARY.md`
</output>
