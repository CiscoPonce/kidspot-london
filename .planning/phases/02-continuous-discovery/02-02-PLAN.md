---
phase: 02-continuous-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/scripts/cron-agent.js
  - backend/db/schema.sql
autonomous: true
requirements:
  - DISCOVER-02
user_setup: []
must_haves:
  truths:
    - "Venues are correctly categorized by type (softplay, community_hall, park, other)"
    - "Google Places types are mapped to our venue types"
    - "OSM tags are mapped to our venue types"
    - "CSV data types are mapped to our venue types"
    - "Categorization logic is consistent across all sources"
    - "Uncategorized venues are logged for review"
  artifacts:
    - path: "backend/scripts/cron-agent.js"
      provides: "Cron agent with categorization logic"
      contains: "mapVenueType(), categorizeVenue()"
    - path: "backend/db/schema.sql"
      provides: "Database schema with type field"
      contains: "type TEXT NOT NULL"
  key_links:
    - from: "backend/scripts/cron-agent.js"
      to: "venue type field"
      via: "mapVenueType function"
      pattern: "mapVenueType(googleTypes)"
---

<objective>
Build venue categorization logic to classify venues by type (softplay, community_hall, park, other) from multiple sources.

Purpose: Ensure all venues are correctly categorized regardless of data source. Map external type systems (Google Places, OSM, CSV) to our internal venue types for consistent filtering and search.

Output: Working categorization logic that correctly classifies venues from all sources.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/02-continuous-discovery/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Google Places type mapping</name>
  <files>backend/scripts/cron-agent.js</files>
  <action>Update backend/scripts/cron-agent.js to add mapVenueType function for Google Places:
1. Create type mapping object:
   - amusement_park → softplay
   - bowling_alley → other
   - community_center → community_hall
   - gym → other
   - park → park
   - playground → park
   - stadium → other
   - swimming_pool → other
   - tourist_attraction → other
   - zoo → other
2. Function takes array of Google Places types
3. Returns our venue type based on first match
4. Returns 'other' if no match found
5. Logs unmapped types for review

This ensures Google Places venues are correctly categorized.</action>
  <verify>mapVenueType function correctly maps Google Places types</verify>
  <done>Google Places type mapping implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement OSM tag mapping</name>
  <files>backend/scripts/discovery/osm-discovery.js</files>
  <action>Update backend/scripts/discovery/osm-discovery.js to add OSM tag mapping:
1. Create OSM tag mapping object:
   - leisure=fitness_centre → softplay
   - amenity=community_centre → community_hall
   - leisure=park → park
   - leisure=playground → park
   - leisure=stadium → other
   - amenity=gym → other
2. Function takes OSM tags object
3. Returns our venue type based on tag matches
4. Returns 'other' if no match found
5. Logs unmapped tags for review

This ensures OSM venues are correctly categorized.</action>
  <verify>OSM tag mapping correctly maps OSM tags to venue types</verify>
  <done>OSM tag mapping implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement CSV type mapping</name>
  <files>backend/scripts/import-london-datastore.js</files>
  <action>Update backend/scripts/import-london-datastore.js to add CSV type mapping:
1. Create CSV type mapping object:
   - leisure centre → softplay
   - soft play → softplay
   - softplay → softplay
   - community hall → community_hall
   - village hall → community_hall
   - park → park
   - playground → park
   - sports centre → other
   - gym → other
   - swimming pool → other
2. Function normalizes CSV type string
3. Returns our venue type based on match
4. Returns 'other' if no match found
5. Logs unmapped types for review

This ensures CSV venues are correctly categorized.</action>
  <verify>CSV type mapping correctly maps CSV types to venue types</verify>
  <done>CSV type mapping implemented</done>
</task>

<task type="auto">
  <name>Task 4: Test categorization across all sources</name>
  <files>backend/scripts/test-categorization.js</files>
  <action>Create backend/scripts/test-categorization.js that:
1. Tests Google Places type mapping with various type combinations
2. Tests OSM tag mapping with various tag combinations
3. Tests CSV type mapping with various type strings
4. Verifies that all mappings return correct venue types
5. Verifies that unmapped types return 'other'
6. Reports test results

This test script validates that categorization works correctly across all sources.</action>
  <verify>node backend/scripts/test-categorization.js passes all tests</verify>
  <done>Categorization tested across all sources</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type Mapping Functions → Database | Categorized venue data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Type Mappings | mitigate | Type mappings are version-controlled in code |
| T-02-02-02 | Information Disclosure | Type Data | accept | Venue types are public information |

**Critical Mitigations for This Plan:**
- Type mappings are version-controlled in code
- All mappings are consistent across sources
- Unmapped types are logged for review
- Default to 'other' for unmapped types
</threat_model>

<verification>
- Google Places type mapping is implemented
- OSM tag mapping is implemented
- CSV type mapping is implemented
- All mappings return correct venue types
- Unmapped types return 'other'
- Test script validates all mappings
</verification>

<success_criteria>
- Google Places type mapping is complete
- OSM tag mapping is complete
- CSV type mapping is complete
- All mappings are consistent
- Unmapped types are logged
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-continuous-discovery/02-02-SUMMARY.md`
</output>
