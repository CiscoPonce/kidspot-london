---
phase: 02-continuous-discovery
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
files_modified:
  - backend/scripts/cron-agent.js
  - backend/db/schema.sql
autonomous: true
requirements:
  - DISCOVER-03
user_setup: []
must_haves:
  truths:
    - "Permanently closed venues are detected via Google Places API"
    - "Permanently closed venues are marked as inactive"
    - "Inactive venues are excluded from search results"
    - "Deactivation is reversible (venue can be reactivated)"
    - "Deactivation is logged for auditability"
  artifacts:
    - path: "backend/scripts/cron-agent.js"
      provides: "Cron agent with venue deactivation"
      contains: "deactivate_venue(), updateVenue()"
    - path: "backend/db/schema.sql"
      provides: "Database schema with is_active field and index"
      contains: "is_active BOOLEAN DEFAULT TRUE, idx_venues_active"
  key_links:
    - from: "backend/scripts/cron-agent.js"
      to: "deactivate_venue function"
      via: "SQL query"
      pattern: "SELECT deactivate_venue($1)"
    - from: "backend/db/schema.sql"
      to: "is_active index"
      via: "CREATE INDEX"
      pattern: "CREATE INDEX idx_venues_active"
---

<objective>
Implement venue deactivation for permanently closed venues to maintain data quality.

Purpose: Detect permanently closed venues via Google Places API and mark them as inactive. Inactive venues are excluded from search results but can be reactivated if they reopen. Ensures data quality and user experience.

Output: Working venue deactivation that detects closed venues and marks them inactive.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/02-continuous-discovery/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deactivate_venue function</name>
  <files>backend/db/schema.sql</files>
  <action>Update backend/db/schema.sql to add deactivate_venue function:
1. Function takes venue_id parameter
2. Updates is_active to FALSE
3. Logs deactivation for auditability
4. Returns VOID

This function allows marking venues as inactive while preserving their data.</action>
  <verify>deactivate_venue function marks venue as inactive</verify>
  <done>deactivate_venue function implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement permanently closed detection</name>
  <files>backend/scripts/cron-agent.js</files>
  <action>Update backend/scripts/cron-agent.js to add permanently closed detection:
1. In updateVenue function, check Google Places API response
2. If permanently_closed flag is true:
   - Call deactivate_venue function
   - Log deactivation
   - Return status: 'deactivated'
3. If venue is still open:
   - Update venue information
   - Return status: 'updated'

This ensures permanently closed venues are detected and marked inactive.</action>
  <verify>Permanently closed venues are detected and deactivated</verify>
  <done>Permanently closed detection implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add is_active index</name>
  <files>backend/db/schema.sql</files>
  <action>Update backend/db/schema.sql to add is_active index:
1. Create partial index on is_active column
2. Index only includes active venues (is_active = TRUE)
3. Index supports efficient search queries
4. Index is used by search_venues_by_radius function

This index ensures efficient search queries that only return active venues.</action>
  <verify>is_active index is created and used by search queries</verify>
  <done>is_active index added</done>
</task>

<task type="auto">
  <name>Task 4: Update search to exclude inactive venues</name>
  <files>backend/db/schema.sql</files>
  <action>Update backend/db/schema.sql search_venues_by_radius function:
1. Add WHERE clause: WHERE v.is_active = TRUE
2. Ensure only active venues are returned
3. Maintain sponsor ranking for active venues

This ensures inactive venues are excluded from search results.</action>
  <verify>search_venues_by_radius excludes inactive venues</verify>
  <done>Search updated to exclude inactive venues</done>
</task>

<task type="auto">
  <name>Task 5: Test venue deactivation</name>
  <files>backend/scripts/test-deactivation.js</files>
  <action>Create backend/scripts/test-deactivation.js that:
1. Inserts test venue
2. Simulates permanently closed venue
3. Runs cron agent on test venue
4. Verifies that venue is marked as inactive
5. Verifies that venue is excluded from search results
6. Verifies that venue can be reactivated
7. Reports test results

This test script validates that venue deactivation works correctly.</action>
  <verify>node backend/scripts/test-deactivation.js passes all tests</verify>
  <done>Venue deactivation tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Places API → Deactivation | Permanently closed flag |
| Deactivation → Database | Update is_active field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Deactivation Logic | mitigate | Deactivation is logged and reversible |
| T-02-04-02 | Information Disclosure | Inactive Venue Data | accept | Venue data is public information |

**Critical Mitigations for This Plan:**
- Deactivation is logged for auditability
- Deactivation is reversible (venue can be reactivated)
- Inactive venues are excluded from search
- is_active index ensures efficient filtering
</threat_model>

<verification>
- deactivate_venue function is implemented
- Permanently closed detection is implemented
- is_active index is created
- Inactive venues are excluded from search
- Deactivation is logged
- Deactivation is reversible
- Test script validates all functionality
</verification>

<success_criteria>
- deactivate_venue function is complete
- Permanently closed detection is complete
- is_active index is created
- Inactive venues are excluded from search
- Deactivation is logged
- Deactivation is reversible
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-continuous-discovery/02-04-SUMMARY.md`
</output>
