---
phase: 02-continuous-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/cron-agent.js
  - backend/package.json
autonomous: true
requirements:
  - CRON-01
  - DISCOVER-01
user_setup:
  - service: cron_scheduling
    why: "Schedule cron agent to run periodically"
    env_vars: []
    dashboard_config:
      - task: "Setup cron job to run cron-agent.js"
        location: "crontab -e or systemd timer"
        details: "Run every 24 hours: 0 2 * * * cd /path/to/kidspot/backend && npm run cron"

must_haves:
  truths:
    - "Cron agent runs periodically (configurable schedule)"
    - "Cron agent processes venues that need scraping (stale data)"
    - "Cron agent updates venue information from sources"
    - "Cron agent marks permanently closed venues as inactive"
    - "Cron agent prioritizes sponsored venues for updates"
    - "Cron agent respects API rate limits"
    - "Cron agent logs all operations (processed, updated, deactivated, errors)"
  artifacts:
    - path: "backend/scripts/cron-agent.js"
      provides: "Cron agent for continuous venue scraping"
      contains: "runCronAgent(), processStaleVenues(), updateVenue()"
    - path: "backend/package.json"
      provides: "Node.js dependencies and cron script"
      contains: "\"cron\": \"node scripts/cron-agent.js\""
  key_links:
    - from: "backend/scripts/cron-agent.js"
      to: "PostgreSQL database"
      via: "pg client"
      pattern: "pool.query"
    - from: "backend/scripts/cron-agent.js"
      to: "Google Places API"
      via: "HTTP request"
      pattern: "axios.get"
    - from: "backend/scripts/cron-agent.js"
      to: "get_venues_needing_scrape function"
      via: "SQL query"
      pattern: "SELECT * FROM get_venues_needing_scrape"
---

<objective>
Implement cron agent for continuous venue discovery, categorization, and data freshness.

Purpose: Build automated system that periodically discovers new venues, updates existing venue information, categorizes venues by type, and marks inactive venues. Ensures data stays fresh and sponsored venues are prioritized.

Output: Working cron agent that runs periodically, processes stale venues, updates information from sources, and maintains data freshness.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/01-data-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cron agent script</name>
  <files>backend/scripts/cron-agent.js</files>
  <action>Create backend/scripts/cron-agent.js that:
1. Connects to PostgreSQL using DATABASE_URL
2. Gets venues that need scraping using get_venues_needing_scrape function
3. Processes venues in batches (configurable batch size)
4. For each venue:
   - Fetches updated information from source (Google Places, OSM)
   - Updates venue type if changed
   - Checks if venue is permanently closed
   - Marks inactive venues as inactive
   - Updates last_scraped timestamp
5. Prioritizes sponsored venues (gold > silver > bronze > none)
6. Logs progress (processed, updated, deactivated, errors)
7. Handles errors gracefully (API failures, timeouts)
8. Respects API rate limits (add delays between requests)
9. Reports summary statistics

Cron agent configuration:
- Stale threshold: 24 hours (configurable)
- Batch size: 50 venues
- API rate limits: 1 request per second for Google Places
- Priority order: sponsored venues first

Google Places API integration:
- Fetch venue details using place_id
- Check for permanently_closed flag
- Update venue type based on types
- Handle API errors gracefully</action>
  <verify>node backend/scripts/cron-agent.js processes stale venues without errors</verify>
  <done>Cron agent script created and tested successfully</done>
</task>

<task type="auto">
  <name>Task 2: Add cron script to package.json</name>
  <files>backend/package.json</files>
  <action>Update backend/package.json to add cron script:
- Add "cron": "node scripts/cron-agent.js" to scripts section
- Ensure all dependencies are present (pg, axios, dotenv)

This allows running the cron agent with: npm run cron</action>
  <verify>npm run cron executes successfully</verify>
  <done>Cron script added to package.json</done>
</task>

<task type="auto">
  <name>Task 3: Test cron agent with sample data</name>
  <files>backend/scripts/test-cron.js</files>
  <action>Create backend/scripts/test-cron.js that:
1. Inserts test venues with different last_scraped timestamps
2. Runs cron agent on test data
3. Verifies that stale venues are updated
4. Verifies that sponsored venues are prioritized
5. Verifies that permanently closed venues are deactivated
6. Reports test results

This test script validates that the cron agent works correctly with various scenarios.</action>
  <verify>node backend/scripts/test-cron.js passes all tests</verify>
  <done>Cron agent tested with sample data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron Agent → Database | Automated data updates |
| Cron Agent → Google Places API | Periodic venue data refresh |
| Cron Agent → OSM API | Periodic venue data refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | Cron Agent | mitigate | Validate API responses, use official APIs |
| T-02-01-02 | Tampering | Database Updates | mitigate | All updates use parameterized queries |
| T-02-01-03 | Repudiation | Cron Logs | mitigate | Log all cron operations (updated, deactivated, errors) |
| T-02-01-04 | Information Disclosure | Venue Data | accept | Venue data is public information |
| T-02-01-05 | Denial of Service | Google Places API | mitigate | Respect rate limits, add delays between requests |
| T-02-01-06 | Elevation of Privilege | Database Updates | mitigate | Use parameterized queries, validate all data before update |

**Critical Mitigations for This Plan:**
- Validate API responses before updating database
- Use parameterized queries for all database updates
- Respect API rate limits (Google Places, OSM)
- Log all cron operations for auditability
- Handle API failures gracefully (skip venue, log error)
- Prioritize sponsored venues for updates
</threat_model>

<verification>
- backend/scripts/cron-agent.js exists and runs without errors
- backend/scripts/cron-agent.js processes stale venues
- backend/scripts/cron-agent.js updates venue information
- backend/scripts/cron-agent.js marks inactive venues
- backend/scripts/cron-agent.js prioritizes sponsored venues
- backend/scripts/cron-agent.js respects API rate limits
- backend/scripts/cron-agent.js logs all operations
- backend/package.json contains cron script
- Test script validates cron agent functionality
</verification>

<success_criteria>
- Cron agent script is complete
- Cron agent processes stale venues
- Cron agent updates venue information from sources
- Cron agent marks permanently closed venues as inactive
- Cron agent prioritizes sponsored venues for updates
- Cron agent respects API rate limits
- Cron agent logs all operations
- Cron script is added to package.json
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-continuous-discovery/02-01-SUMMARY.md`
</output>
