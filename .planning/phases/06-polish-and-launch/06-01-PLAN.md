---
phase: 06-polish-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/package.json, frontend/package.json, backend/scripts/profile-search.js]
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Backend search endpoint is load-tested with autocannon"
    - "Performance bottlenecks are identified using Clinic.js"
    - "Frontend Core Web Vitals are measured in production-like environment"
  artifacts:
    - path: "backend/scripts/profile-search.js"
      provides: "Automated profiling script"
  key_links:
    - from: "autocannon"
      to: "/api/search"
      via: "HTTP load generation"
---

<objective>
Profile and optimize the performance of the KidSpot platform to ensure sub-2s load times on mobile devices.

Purpose: Meet the PERF-01 requirement and ensure a smooth user experience for launch.
Output: Performance baseline reports and targeted optimizations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-launch/06-RESEARCH.md
@.planning/phases/06-polish-and-launch/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup performance profiling tools</name>
  <files>backend/package.json, frontend/package.json</files>
  <action>
    Install performance testing and profiling tools as recommended in RESEARCH.md.
    - Backend: Install `clinic` and `autocannon` as devDependencies.
    - Frontend: Install `autocannon` as devDependency.
    - Add scripts to `package.json` for profiling (e.g., `perf:doctor`, `perf:flame`).
  </action>
  <verify>
    <automated>npm list -g clinic autocannon || (cd backend && npm list clinic autocannon) && (cd frontend && npm list autocannon)</automated>
  </verify>
  <done>Tools installed and available for use.</done>
</task>

<task type="auto">
  <name>Task 2: Profile Backend Search API</name>
  <files>backend/scripts/profile-search.js</files>
  <action>
    Create a script to run Clinic Doctor and Flame against the search API under load from Autocannon.
    - Target: `GET /api/search?q=SE1&radius=5` (high-traffic pattern).
    - Use Autocannon to generate 50 concurrent connections for 20 seconds.
    - Analyze results for slow PostGIS queries or event loop blockages.
    - Per D-05 (discretion), apply immediate optimizations if "low-hanging fruit" (e.g., missing indexes or redundant queries) are found.
  </action>
  <verify>
    <automated>node backend/scripts/profile-search.js</automated>
  </verify>
  <done>Backend profiling completed and bottlenecks identified.</done>
</task>

<task type="auto">
  <name>Task 3: Profile Frontend Hydration and LCP</name>
  <files>frontend/package.json</files>
  <action>
    Measure Next.js performance using production build.
    - Run `npm run build` and `npm run start` in the frontend.
    - Use Lighthouse (via CLI or browser) to measure LCP, CLS, and TBT.
    - Verify image optimization is effective for venue cards.
    - Check for hydration mismatches (Pitfall 1 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd frontend && npm run build && echo "Build successful - check Lighthouse scores manually if needed, but automated build ensures production-readiness."</automated>
  </verify>
  <done>Frontend performance baselined and hydration verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public -> API | Load testing simulates high-volume public traffic. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Denial of Service | API Search | mitigate | Use profiling to find and fix unoptimized queries that cause high CPU/RAM usage. |
</threat_model>

<verification>
Verify that the application build is successful and profiling scripts run without error.
</verification>

<success_criteria>
- Backend profiling scripts successfully generate Clinic.js reports.
- Frontend production build completes without errors.
- Any identified "low-hanging fruit" performance issues are addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-launch/06-01-SUMMARY.md`
</output>
