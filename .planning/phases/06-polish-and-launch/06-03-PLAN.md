---
phase: 06-polish-and-launch
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [frontend/package.json, frontend/src/app/layout.tsx, frontend/next.config.js, frontend/src/hooks/use-search.tsx]
autonomous: true
requirements: [MONITOR-01]

must_haves:
  truths:
    - "Plausible Analytics is integrated with Next.js"
    - "Analytics script is proxied to bypass adblockers"
    - "Search events and radius changes are tracked"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Global Plausible script integration"
    - path: "frontend/next.config.js"
      provides: "Analytics proxy configuration"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "PlausibleProvider"
      via: "React Component"
---

<objective>
Configure Plausible Analytics to provide privacy-first telemetry for tracking search density, fallback rates, and user engagement.

Purpose: Meet the MONITOR-01 requirement with a privacy-compliant solution.
Output: Integrated Plausible script and custom event tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-launch/06-RESEARCH.md
@.planning/phases/06-polish-and-launch/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and Configure Plausible Provider</name>
  <files>frontend/package.json, frontend/src/app/layout.tsx</files>
  <action>
    Install `next-plausible` and add it to the root layout.
    - `cd frontend && npm install next-plausible`.
    - Wrap the `<html>` content in `src/app/layout.tsx` with `<PlausibleProvider>`.
    - Configure with `domain="kidspot.london"`, `trackOutboundLinks={true}`, and `proxy={true}` as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>grep "PlausibleProvider" frontend/src/app/layout.tsx && grep "next-plausible" frontend/package.json</automated>
  </verify>
  <done>Plausible provider integrated into frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Analytics Proxy</name>
  <files>frontend/next.config.js</files>
  <action>
    Setup rewrites in `next.config.js` to proxy the Plausible script and API.
    - This allows the analytics to bypass adblockers and enhances data accuracy.
    - Use the standard `next-plausible` proxy pattern.
  </action>
  <verify>
    <automated>grep "withPlausibleProxy" frontend/next.config.js</automated>
  </verify>
  <done>Analytics proxy configured in Next.js.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Custom Event Tracking</name>
  <files>frontend/src/hooks/use-search.tsx</files>
  <action>
    Add custom event tracking for core user actions.
    - Use `usePlausible` hook from `next-plausible`.
    - Track `Search` event when a search is executed (include query and radius).
    - Track `RadiusChange` event when the slider is moved.
    - Track `FallbackTriggered` event when the Brave Search API is used (per success criteria in ROADMAP).
  </action>
  <verify>
    <automated>grep "usePlausible" frontend/src/hooks/use-search.tsx</automated>
  </verify>
  <done>Custom event tracking implemented for search actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Plausible | Telemetry data sent from user's browser to Plausible server. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Privacy Violation | Analytics | mitigate | Use Plausible (no cookies, no PII, GDPr compliant) to ensure user privacy. |
</threat_model>

<verification>
Check `next.config.js` for proxy settings and `layout.tsx` for the provider.
</verification>

<success_criteria>
- `next-plausible` is installed and integrated.
- Plausible provider is configured correctly with the project domain.
- Analytics proxy is setup in `next.config.js`.
- Custom events for search are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-launch/06-03-SUMMARY.md`
</output>
