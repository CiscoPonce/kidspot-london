---
phase: 05-seo-detail-pages-weeks-9-10
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified: [frontend/package.json, frontend/src/app/layout.tsx, frontend/src/app/venue/[slug]/page.tsx, frontend/src/app/venues-in/[borough]/page.tsx, frontend/src/app/venues-by/[type]/page.tsx, frontend/src/app/sitemap.ts, frontend/src/components/venues/share-button.tsx]
autonomous: true
requirements: [SEO-03, ROUTING-01]
---

<objective>
Finalize SEO optimization and social sharing features.

Purpose: Increase organic reach and enable viral sharing.
Output: Dynamic metadata, JSON-LD structured data, XML sitemap, and native social sharing with clipboard fallback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-CONTEXT.md
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-RESEARCH.md
@frontend/src/app/layout.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: SEO Metadata & JSON-LD</name>
  <files>frontend/src/app/venue/[slug]/page.tsx, frontend/src/app/venues-in/[borough]/page.tsx, frontend/src/app/venues-by/[type]/page.tsx</files>
  <action>
    - Implement `generateMetadata()` in the Venue Detail page and Landing pages to generate dynamic titles, descriptions, and OpenGraph tags.
    - Inject `LocalBusiness` JSON-LD structured data into the Venue Detail page for Google Rich Results.
    - Ensure a static branded placeholder image is used for OpenGraph (per D-02).
  </action>
  <verify>
    <automated>grep "generateMetadata" frontend/src/app/venue/\[slug\]/page.tsx && grep "application/ld+json" frontend/src/app/venue/\[slug\]/page.tsx</automated>
  </verify>
  <done>Dynamic SEO metadata and structured data implemented.</done>
</task>

<task type="auto">
  <name>Task 4.2: Dynamic Sitemap</name>
  <files>frontend/src/app/sitemap.ts</files>
  <action>
    - Create `frontend/src/app/sitemap.ts` to dynamically generate a sitemap.
    - Include:
      - Homepage (`/`)
      - All borough landing pages (`/venues-in/[borough]`)
      - All category landing pages (`/venues-by/[type]`)
      - All venue detail pages (`/venue/[slug]`) - fetch all slugs from the backend (may need a new simple `/api/search/slugs` endpoint or similar if there are many thousands).
    - Prioritize sponsored venues (gold/silver) in the list order.
  </action>
  <verify>
    <automated>ls frontend/src/app/sitemap.ts</automated>
  </verify>
  <done>Dynamic XML sitemap implemented and inclusive of all programmatic routes.</done>
</task>

<task type="auto">
  <name>Task 4.3: Social Sharing & Toasts</name>
  <files>frontend/package.json, frontend/src/app/layout.tsx, frontend/src/components/venues/share-button.tsx</files>
  <action>
    - Install `sonner` (v1.5.0) in the frontend and add `<Toaster />` to `frontend/src/app/layout.tsx`.
    - Create `ShareButton` component in `frontend/src/components/venues/share-button.tsx`.
    - Logic: Use Web Share API if available; otherwise, fallback to copying the current URL to the clipboard and showing a `sonner` success toast.
    - Add the `ShareButton` to the `VenueDetailContent` and Standalone Venue Page.
  </action>
  <verify>
    <automated>cd frontend && npm list sonner && ls frontend/src/components/venues/share-button.tsx</automated>
  </verify>
  <done>Native social sharing and toast notifications implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- client → API: Slug list for sitemap.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Metadata injection | mitigate | Use Next.js `generateMetadata` which handles escaping; use `JSON.stringify` for JSON-LD. |
</threat_model>

<must_haves>
  truths:
    - "Google Search Console can see a sitemap with all venues"
    - "Sharing a venue page on social media shows the correct title/description"
    - "Clicking share on desktop copies the link and shows a toast notification"
  artifacts:
    - path: "frontend/src/app/sitemap.ts"
      provides: "Dynamic sitemap generation"
    - path: "frontend/src/components/venues/share-button.tsx"
      provides: "Social sharing functionality"
  key_links:
    - from: "ShareButton"
      to: "navigator.share"
      via: "Web API"
</must_haves>

<success_criteria>
- `/sitemap.xml` returns a valid list of URLs.
- Standalone pages have correct `<title>` and `<meta>` tags.
- Share button works on both mobile (native) and desktop (clipboard + toast).
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-detail-pages-weeks-9-10/05-04-SUMMARY.md`
</output>
