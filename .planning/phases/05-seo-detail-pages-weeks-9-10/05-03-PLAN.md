---
phase: 05-seo-detail-pages-weeks-9-10
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: [frontend/src/lib/constants.ts, backend/src/routes/search.js, frontend/src/app/venues-in/[borough]/page.tsx, frontend/src/app/venues-by/[type]/page.tsx]
autonomous: true
requirements: [SEO-02]
---

<objective>
Generate programmatic SEO landing pages for boroughs and venue types.

Purpose: Capture long-tail search traffic for location-based and category-based searches.
Output: Dynamic landing pages for 32 London Boroughs and core venue categories.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-CONTEXT.md
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-RESEARCH.md
@backend/src/routes/search.js
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Canonical Boroughs & Backend Filter</name>
  <files>frontend/src/lib/constants.ts, backend/src/routes/search.js</files>
  <action>
    - Create `frontend/src/lib/constants.ts` with the list of 33 London areas (32 boroughs + City of London) as specified in CONTEXT.md.
    - Update `backend/src/routes/search.js`:
      - Add support for an optional `borough` query parameter to `GET /api/search/venues`.
      - If `borough` is provided, filter results by the `borough` column (case-insensitive).
  </action>
  <verify>
    <automated>grep "Camden" frontend/src/lib/constants.ts && curl -s "http://localhost:4000/api/search/venues?borough=Camden" | grep "success\":true"</automated>
  </verify>
  <done>Canonical area list established and backend supports filtering by borough.</done>
</task>

<task type="auto">
  <name>Task 3.2: Borough Landing Pages</name>
  <files>frontend/src/app/venues-in/[borough]/page.tsx</files>
  <action>
    - Create `frontend/src/app/venues-in/[borough]/page.tsx` as a Server Component.
    - Logic: Use the `borough` param to fetch venues in that area from the backend.
    - Render a list of venues with an SEO-optimized heading (e.g., "Best child-friendly venues in [Borough]").
    - Include links back to the main search map with the borough's approximate coordinates (can be hardcoded or derived).
  </action>
  <verify>
    <automated>ls frontend/src/app/venues-in/\[borough\]/page.tsx</automated>
  </verify>
  <done>Dynamic borough landing pages implemented.</done>
</task>

<task type="auto">
  <name>Task 3.3: Category Landing Pages</name>
  <files>frontend/src/app/venues-by/[type]/page.tsx</files>
  <action>
    - Create `frontend/src/app/venues-by/[type]/page.tsx` as a Server Component.
    - Logic: Fetch venues by `type` (softplay, community_hall, etc.) across all areas.
    - Render a list of venues with an SEO-optimized heading (e.g., "Top [Type] venues for kids in London").
  </action>
  <verify>
    <automated>ls frontend/src/app/venues-by/\[type\]/page.tsx</automated>
  </verify>
  <done>Dynamic category landing pages implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- client → API: Borough and type parameters are untrusted.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | URL Parameters | mitigate | Validate `borough` and `type` against canonical lists in the API. |
</threat_model>

<must_haves>
  truths:
    - "Landing pages exist for every London borough"
    - "Landing pages exist for every venue category"
    - "Pages show a list of relevant venues and link to their detail pages"
  artifacts:
    - path: "frontend/src/app/venues-in/[borough]/page.tsx"
      provides: "Borough landing pages"
    - path: "frontend/src/lib/constants.ts"
      provides: "Canonical borough list"
  key_links:
    - from: "/venues-in/[borough]"
      to: "/venue/[slug]"
      via: "List navigation"
</must_haves>

<success_criteria>
- Navigation to `/venues-in/islington` shows venues in Islington.
- Navigation to `/venues-by/soft-play` shows soft play venues.
- Each landing page is server-rendered for SEO crawlability.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-detail-pages-weeks-9-10/05-03-SUMMARY.md`
</output>
