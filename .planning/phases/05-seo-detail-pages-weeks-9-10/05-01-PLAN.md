---
phase: 05-seo-detail-pages-weeks-9-10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/package.json, backend/db/migrations/002_add_slugs.sql, backend/src/utils/slug.js, backend/src/routes/search.js, backend/scripts/migrations/backfill-slugs.js, backend/scripts/import-london-datastore.js]
autonomous: true
requirements: [SEO-01]
---

<objective>
Prepare the database and backend for slug-based routing by adding slug and borough columns, implementing slug generation logic, and updating API endpoints.

Purpose: Enable SEO-friendly URLs for venues instead of numeric IDs.
Output: Migrated database, slug generation utility, backfilled slugs, and slug-aware API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-CONTEXT.md
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-RESEARCH.md
@backend/db/schema.sql
@backend/src/routes/search.js
@backend/scripts/import-london-datastore.js
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Backend Dependencies & Slug Utility</name>
  <files>backend/package.json, backend/src/utils/slug.js</files>
  <action>
    - Install `slugify` (v1.6.6) in the backend.
    - Create `backend/src/utils/slug.js` exporting `generateSlug(name, borough)`.
    - Logic: `slugify(name + '-' + borough, { lower: true, strict: true })`.
    - Handle collisions: The utility should support appending a 4-character random hash (using `crypto.randomBytes(2).toString('hex')`) if a flag is passed or if we want to ensure uniqueness.
  </action>
  <verify>
    <automated>cd backend && npm list slugify && node -e "const {generateSlug} = require('./src/utils/slug'); console.log(generateSlug('Starbucks', 'Islington'))" | grep "starbucks-islington"</automated>
  </verify>
  <done>`slugify` installed and utility correctly generates slugs.</done>
</task>

<task type="auto">
  <name>Task 1.2: Database Migration & Function Updates</name>
  <files>backend/db/migrations/002_add_slugs.sql, backend/db/schema.sql</files>
  <action>
    - Create migration `backend/db/migrations/002_add_slugs.sql`:
      - Add `slug` (TEXT) and `borough` (TEXT) columns to `venues`.
      - Add UNIQUE index to `slug`.
    - Update `backend/db/schema.sql` (or relevant functions) to:
      - Include `slug` and `borough` in `insert_venue_if_not_duplicate` parameters and insertion.
      - Update `search_venues_by_radius` to return the `slug` column.
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "\d venues" | grep -E "slug|borough"</automated>
  </verify>
  <done>Database schema updated with slug and borough columns and unique index.</done>
</task>

<task type="auto">
  <name>Task 1.3: Backfill Slugs & Update Import Script</name>
  <files>backend/scripts/migrations/backfill-slugs.js, backend/scripts/import-london-datastore.js</files>
  <action>
    - Create `backend/scripts/migrations/backfill-slugs.js`:
      - Iterate through existing venues.
      - Generate slugs using `generateSlug(name, 'London')` (since existing data might lack borough).
      - Handle collisions by appending a hash and retrying until unique.
      - Update the database records.
    - Update `backend/scripts/import-london-datastore.js`:
      - Extract `borough_name` (or similar) from CSV rows.
      - Pass `borough` to `insertVenue` / `insert_venue_if_not_duplicate`.
      - Generate slug before insertion using the new utility.
  </action>
  <verify>
    <automated>node backend/scripts/migrations/backfill-slugs.js && psql $DATABASE_URL -c "SELECT name, slug FROM venues LIMIT 5" | grep -v "(0 rows)"</automated>
  </verify>
  <done>Existing venues have unique slugs and import script stores slugs/boroughs for new data.</done>
</task>

<task type="auto">
  <name>Task 1.4: Slug Resolution API</name>
  <files>backend/src/routes/search.js</files>
  <action>
    - Implement `GET /api/search/venues/slug/:slug/details` in `backend/src/routes/search.js`.
    - Logic: Similar to the ID details route but query by `slug`.
    - Return full details including agentic search results from Google/OSM.
    - Ensure regular search results (`/api/search/venues`) now include the `slug` field.
  </action>
  <verify>
    <automated>curl -s "http://localhost:4000/api/search/venues/slug/$(psql $DATABASE_URL -t -c "SELECT slug FROM venues LIMIT 1" | tr -d ' ')/details" | grep "success\":true"</automated>
  </verify>
  <done>API can resolve venue details via slug and search results include slugs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- client → API: Slug parameter is untrusted input.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Slug parameter | mitigate | Sanitize slug parameter in API route; use parameterized SQL queries. |
| T-05-01-02 | Information Disclosure | Slug generation | mitigate | Do not include sensitive info in slugs; use public borough/name only. |
</threat_model>

<must_haves>
  truths:
    - "Venues have unique, SEO-friendly slugs in the database"
    - "Backend API can resolve venue details using a slug"
    - "New imports automatically generate and store slugs"
  artifacts:
    - path: "backend/src/utils/slug.js"
      provides: "Slug generation logic"
    - path: "backend/db/migrations/002_add_slugs.sql"
      provides: "Schema update"
  key_links:
    - from: "GET /api/search/venues/slug/:slug/details"
      to: "venues table"
      via: "SQL query by slug"
</must_haves>

<success_criteria>
- `venues` table has `slug` and `borough` columns.
- All existing venues have a unique slug.
- `GET /api/search/venues/slug/:slug/details` returns venue details.
- Search API results include the venue slug.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-detail-pages-weeks-9-10/05-01-SUMMARY.md`
</output>
