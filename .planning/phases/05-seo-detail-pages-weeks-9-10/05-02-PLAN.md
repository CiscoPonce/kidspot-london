---
phase: 05-seo-detail-pages-weeks-9-10
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [frontend/src/lib/api.ts, frontend/src/components/venues/venue-detail-content.tsx, frontend/src/app/venue/[slug]/page.tsx, frontend/src/components/venues/venue-card.tsx, frontend/src/components/venues/venue-list-item.tsx, frontend/src/components/modals/venue-detail-modal.tsx]
autonomous: true
requirements: [SEO-01]
---

<objective>
Implement dynamic standalone venue pages and internal navigation from search results.

Purpose: Provide crawlable, shareable pages for every venue.
Output: Standalone venue pages, refactored UI components, and updated internal links.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-CONTEXT.md
@.planning/phases/05-seo-detail-pages-weeks-9-10/05-RESEARCH.md
@frontend/src/lib/api.ts
@frontend/src/components/modals/venue-detail-modal.tsx
@frontend/src/components/venues/venue-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: API Client & Types Update</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
    - Update `Venue` and `VenueDetails` interfaces to include `slug: string` and `borough?: string`.
    - Implement `getVenueBySlug(slug: string)` which calls `/api/search/venues/slug/${slug}/details`.
  </action>
  <verify>
    <automated>grep -E "slug: string" frontend/src/lib/api.ts && grep "getVenueBySlug" frontend/src/lib/api.ts</automated>
  </verify>
  <done>Frontend API client and types updated for slug support.</done>
</task>

<task type="auto">
  <name>Task 2.2: Refactor UI for Venue Details</name>
  <files>frontend/src/components/venues/venue-detail-content.tsx, frontend/src/components/modals/venue-detail-modal.tsx</files>
  <action>
    - Extract `ModalContent`, `SponsorBadge`, `TypeBadge`, `ModalLoadingSkeleton`, and `ModalErrorState` from `VenueDetailModal` into a new standalone component `VenueDetailContent` in `frontend/src/components/venues/venue-detail-content.tsx`.
    - Update `VenueDetailModal` to use the new `VenueDetailContent` component.
    - Ensure `VenueDetailContent` can accept `initialVenue` data to support server-side rendering on the standalone page.
  </action>
  <verify>
    <automated>ls frontend/src/components/venues/venue-detail-content.tsx && grep "VenueDetailContent" frontend/src/components/modals/venue-detail-modal.tsx</automated>
  </verify>
  <done>UI components refactored for reuse between modal and standalone page.</done>
</task>

<task type="auto">
  <name>Task 2.3: Implement Standalone Venue Page</name>
  <files>frontend/src/app/venue/[slug]/page.tsx</files>
  <action>
    - Create `frontend/src/app/venue/[slug]/page.tsx` as a Server Component.
    - Fetch venue by slug on the server using `getVenueBySlug`.
    - Render the `VenueDetailContent` component within a page layout (including a "Back to Search" navigation link).
    - Handle 404 state if the venue is not found.
  </action>
  <verify>
    <automated>ls frontend/src/app/venue/\[slug\]/page.tsx</automated>
  </verify>
  <done>Standalone venue page implemented and fetching data by slug.</done>
</task>

<task type="auto">
  <name>Task 2.4: Update Search Result Navigation</name>
  <files>frontend/src/components/venues/venue-card.tsx, frontend/src/components/venues/venue-list-item.tsx</files>
  <action>
    - In `VenueCard` and `VenueListItem`, add a link to `/venue/${venue.slug}` (e.g., an "Info" button or make the title clickable).
    - Maintain the existing modal flow but provide a clear way to navigate to the standalone page for sharing/SEO.
  </action>
  <verify>
    <automated>grep -E "href=.*venue/.*slug" frontend/src/components/venues/venue-card.tsx</automated>
  </verify>
  <done>Search results now link to standalone venue pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None (UI layer)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Slug in URL | accept | Slugs contain only public information (name, area). |
</threat_model>

<must_haves>
  truths:
    - "Navigating to /venue/[slug] displays the correct venue information"
    - "Search result cards link to the standalone venue pages"
    - "The venue detail UI is shared between the modal and the page"
  artifacts:
    - path: "frontend/src/app/venue/[slug]/page.tsx"
      provides: "Dynamic venue page route"
    - path: "frontend/src/components/venues/venue-detail-content.tsx"
      provides: "Reusable venue detail UI"
  key_links:
    - from: "VenueCard"
      to: "/venue/[slug]"
      via: "Next.js Link"
</must_haves>

<success_criteria>
- Standalone venue pages load correctly at `/venue/[slug]`.
- UI is consistent between the detail modal and the standalone page.
- Users can navigate to the standalone page from search results.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-detail-pages-weeks-9-10/05-02-SUMMARY.md`
</output>
