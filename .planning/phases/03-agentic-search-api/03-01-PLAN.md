---
phase: 03-agentic-search-api
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - backend/src/routes/search.js
  - backend/src/server.js
autonomous: true
requirements:
  - API-01
user_setup: []
must_haves:
  truths:
    - "Express API server is running on port 4000"
    - "/api/search/venues endpoint accepts lat, lon, radius_miles, type, limit parameters"
    - "/api/search/venues endpoint returns venues with sponsor ranking"
    - "/api/search/venues endpoint separates sponsored and regular results"
    - "/api/search/venues endpoint includes sponsor metadata"
    - "Spatial queries use search_venues_by_radius function"
    - "Results are ordered by sponsor tier, then distance"
    - "API returns JSON with success, data, and meta fields"
  artifacts:
    - path: "backend/src/routes/search.js"
      provides: "Search API routes with sponsor ranking"
      contains: "router.get('/venues'), search_venues_by_radius"
    - path: "backend/src/server.js"
      provides: "Express server with all routes"
      contains: "app.use('/api/search', searchRoutes)"
  key_links:
    - from: "backend/src/routes/search.js"
      to: "search_venues_by_radius function"
      via: "SQL query"
      pattern: "pool.query('SELECT * FROM search_venues_by_radius"
    - from: "backend/src/routes/search.js"
      to: "sponsor ranking"
      via: "ORDER BY clause"
      pattern: "ORDER BY sponsor_tier, sponsor_priority"
---

<objective>
Create Express API with spatial search endpoint and sponsor-aware result ranking.

Purpose: Build REST API that allows users to search for venues by location and radius, with results ranked by sponsor tier (gold > silver > bronze > none) and distance.

Output: Working Express API with spatial search endpoint that returns sponsor-ranked results.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/01-data-foundation/01-02-PLAN.md
@.planning/phases/02-continuous-discovery/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Express server</name>
  <files>backend/src/server.js</files>
  <action>Create backend/src/server.js that:
1. Imports required modules (express, cors, helmet, rateLimit)
2. Creates Express app
3. Applies security middleware (helmet, cors)
4. Applies rate limiting (60 req/min per IP)
5. Configures body parsing (express.json, express.urlencoded)
6. Adds health check endpoint (/health)
7. Imports and mounts API routes
8. Adds 404 handler
9. Adds error handler
10. Starts server on port 4000

Security features:
- Helmet.js for security headers
- CORS for cross-origin requests
- Rate limiting to prevent abuse
- Input validation
- Error handling</action>
  <verify>Express server starts on port 4000 without errors</verify>
  <done>Express server created and running</done>
</task>

<task type="auto">
  <name>Task 2: Create search API routes</name>
  <files>backend/src/routes/search.js</files>
  <action>Create backend/src/routes/search.js that:
1. Imports required modules (express, pg, axios)
2. Creates Express router
3. Implements /api/search/venues endpoint:
   - Accepts query parameters: lat, lon, radius_miles, type, limit
   - Validates required parameters (lat, lon)
   - Sets defaults (radius_miles=5, limit=50)
   - Calls search_venues_by_radius function
   - Separates sponsored and regular results
   - Returns JSON with success, data, and meta fields
   - Includes sponsor metadata (gold_count, silver_count, bronze_count)
4. Implements /api/search/venues/:id/details endpoint:
   - Gets basic venue info from database
   - Fetches full details from source (agentic search)
   - Returns combined basic + details response

Agentic search features:
- Fetches full details from Google Places API
- Fetches full details from OSM API
- Returns address, phone, website, reviews, photos, opening hours</action>
  <verify>Search API endpoints return correct results with sponsor ranking</verify>
  <done>Search API routes created with sponsor ranking</done>
</task>

<task type="auto">
  <name>Task 3: Test search API</name>
  <files>backend/scripts/test-search-api.js</files>
  <action>Create backend/scripts/test-search-api.js that:
1. Inserts test venues with different sponsor tiers
2. Tests /api/search/venues endpoint with various parameters
3. Verifies that sponsored results appear first
4. Verifies that results are ordered by distance within same tier
5. Verifies that type filtering works correctly
6. Verifies that radius filtering works correctly
7. Tests /api/search/venues/:id/details endpoint
8. Verifies that agentic search fetches full details
9. Reports test results

This test script validates that the search API works correctly with sponsor ranking.</action>
  <verify>node backend/scripts/test-search-api.js passes all tests</verify>
  <done>Search API tested with sponsor ranking</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | SQL queries and data access |
| API → Google Places API | Agentic search for full details |
| API → OSM API | Agentic search for full details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | API Endpoints | mitigate | Implement API authentication in future phases |
| T-03-01-02 | Tampering | API Responses | mitigate | Validate all input parameters |
| T-03-01-03 | Repudiation | API Requests | mitigate | Add request logging in future phases |
| T-03-01-04 | Information Disclosure | Venue Data | accept | Venue data is public information |
| T-03-01-05 | Denial of Service | API Endpoints | mitigate | Rate limiting (60 req/min per IP) |
| T-03-01-06 | Elevation of Privilege | API Endpoints | mitigate | Input validation, parameterized queries |

**Critical Mitigations for This Plan:**
- Rate limiting prevents abuse
- Input validation prevents injection
- Parameterized queries prevent SQL injection
- Security headers via Helmet.js
- CORS configuration
- Error handling prevents information leakage
</threat_model>

<verification>
- Express server starts without errors
- /api/search/venues endpoint accepts all parameters
- /api/search/venues endpoint returns sponsor-ranked results
- Sponsored results appear first
- Results are ordered by distance within same tier
- Type filtering works correctly
- Radius filtering works correctly
- /api/search/venues/:id/details endpoint works
- Agentic search fetches full details
- Test script validates all functionality
</verification>

<success_criteria>
- Express server is running on port 4000
- /api/search/venues endpoint is complete
- /api/search/venues/:id/details endpoint is complete
- Sponsor ranking works correctly
- Results are ordered by tier then distance
- Type filtering works correctly
- Radius filtering works correctly
- Agentic search fetches full details
- Rate limiting is configured
- Security headers are configured
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-search-api/03-01-SUMMARY.md`
</output>
