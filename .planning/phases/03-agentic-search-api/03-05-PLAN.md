---
phase: 03-agentic-search-api
plan: 05
type: execute
wave: 5
depends_on:
  - 03-01
files_modified:
  - backend/src/server.js
autonomous: true
requirements:
  - API-03
user_setup: []
must_haves:
  truths:
    - "Rate limiting is configured (60 req/min per IP)"
    - "Rate limiting prevents abuse"
    - "Security headers are configured via Helmet.js"
    - "HSTS header is set"
    - "NoSniff header is set"
    - "X-Frame-Options header is set"
    - "CORS is configured for production domain"
    - "Input validation is implemented"
    - "Error handling prevents information leakage"
  artifacts:
    - path: "backend/src/server.js"
      provides: "Express server with security middleware"
      contains: "helmet(), rateLimit(), cors()"
  key_links:
    - from: "backend/src/server.js"
      to: "Helmet.js"
      via: "middleware"
      pattern: "app.use(helmet())"
    - from: "backend/src/server.js"
      to: "express-rate-limit"
      via: "middleware"
      pattern: "app.use(limiter)"
    - from: "backend/src/server.js"
      to: "cors"
      via: "middleware"
      pattern: "app.use(cors())"
---

<objective>
Add rate limiting and security headers (Helmet.js) to protect API endpoints from abuse and ensure secure communication.

Purpose: Implement rate limiting to prevent API abuse, add security headers via Helmet.js, configure CORS for production domain, and ensure input validation and error handling.

Output: Secure API with rate limiting, security headers, CORS configuration, and proper error handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/03-agentic-search-api/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting</name>
  <files>backend/src/server.js</files>
  <action>Update backend/src/server.js to add rate limiting:
1. Import rateLimit: const rateLimit = require('express-rate-limit')
2. Create rate limiter:
   - windowMs: 60 * 1000 (1 minute)
   - max: 60 (60 requests per minute)
   - message: 'Too many requests from this IP, please try again later.'
3. Apply rate limiter to /api/ routes:
   - app.use('/api/', limiter)
4. Test rate limiting with multiple requests

Rate limiting configuration:
- Window: 1 minute
- Max requests: 60 per IP
- Custom error message
- Applied to all /api/ routes</action>
  <verify>Rate limiting is configured and working</verify>
  <done>Rate limiting implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add security headers via Helmet.js</name>
  <files>backend/src/server.js</files>
  <action>Update backend/src/server.js to add Helmet.js:
1. Import helmet: const helmet = require('helmet')
2. Apply Helmet.js middleware:
   - app.use(helmet())
3. Configure Helmet.js options:
   - contentSecurityPolicy for production
   - hsts for HTTPS enforcement
   - noSniff for MIME type sniffing
   - frameguard for clickjacking protection

Security headers:
- HSTS: Strict-Transport-Security
- NoSniff: X-Content-Type-Options
- Frameguard: X-Frame-Options
- CSP: Content-Security-Policy</action>
  <verify>Security headers are configured correctly</verify>
  <done>Security headers implemented via Helmet.js</done>
</task>

<task type="auto">
  <name>Task 3: Configure CORS</name>
  <files>backend/src/server.js</files>
  <action>Update backend/src/server.js to configure CORS:
1. Import cors: const cors = require('cors')
2. Configure CORS options:
   - origin: process.env.CORS_ORIGIN || '*' (development)
   - credentials: true
3. Apply CORS middleware:
   - app.use(cors(options))

CORS configuration:
- Development: Allow all origins
- Production: Restrict to production domain
- Credentials: Enabled for cookies/auth</action>
  <verify>CORS is configured correctly</verify>
  <done>CORS configured</done>
</task>

<task type="auto">
  <name>Task 4: Add input validation</name>
  <files>backend/src/routes/search.js</files>
  <action>Update backend/src/routes/search.js to add input validation:
1. In /api/search/venues endpoint:
   - Validate lat parameter (must be number, between -90 and 90)
   - Validate lon parameter (must be number, between -180 and 180)
   - Validate radius_miles parameter (must be number, between 0.1 and 50)
   - Validate type parameter (must be one of: softplay, community_hall, park, other)
   - Validate limit parameter (must be number, between 1 and 100)
   - Return 400 error if validation fails
2. In /api/search/venues/:id/details endpoint:
   - Validate id parameter (must be positive integer)
   - Return 404 error if venue not found

Input validation:
- Type checking
- Range checking
- Required field validation
- Error responses with clear messages</action>
  <verify>Input validation is implemented and working</verify>
  <done>Input validation implemented</done>
</task>

<task type="auto">
  <name>Task 5: Add error handling</name>
  <files>backend/src/server.js</files>
  <action>Update backend/src/server.js to add error handling:
1. Add error handler middleware:
   - Catches all errors
   - Logs error details
   - Returns generic error message to client
   - Returns 500 status code
2. Add 404 handler:
   - Returns 404 for unknown routes
   - Returns JSON error response
3. Add try-catch blocks in route handlers
4. Log errors for debugging

Error handling:
- Generic error messages to prevent information leakage
- Detailed error logging for debugging
- Proper HTTP status codes
- JSON error responses</action>
  <verify>Error handling is implemented and working</verify>
  <done>Error handling implemented</done>
</task>

<task type="auto">
  <name>Task 6: Test security features</name>
  <files>backend/scripts/test-security.js</files>
  <action>Create backend/scripts/test-security.js that:
1. Tests rate limiting:
   - Makes 60 requests within 1 minute
   - Verifies that 61st request is rate limited
   - Verifies that rate limit resets after window
2. Tests security headers:
   - Makes request to /health endpoint
   - Verifies that security headers are present
   - Verifies HSTS header
   - Verifies NoSniff header
   - Verifies X-Frame-Options header
3. Tests CORS:
   - Makes request from different origin
   - Verifies that CORS headers are present
   - Verifies that preflight requests work
4. Tests input validation:
   - Makes request with invalid parameters
   - Verifies that 400 error is returned
   - Verifies that error message is clear
5. Tests error handling:
   - Triggers error in route handler
   - Verifies that 500 error is returned
   - Verifies that error message is generic
6. Reports test results

This test script validates that all security features work correctly.</action>
  <verify>node backend/scripts/test-security.js passes all tests</verify>
  <done>Security features tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rate Limiter → API | Request throttling |
| Helmet.js → API | Security headers |
| CORS → API | Cross-origin requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | API Endpoints | mitigate | Rate limiting prevents brute force |
| T-03-05-02 | Tampering | API Requests | mitigate | Input validation prevents injection |
| T-03-05-03 | Repudiation | API Requests | mitigate | Request logging in future phases |
| T-03-05-04 | Information Disclosure | Error Messages | mitigate | Generic error messages prevent leakage |
| T-03-05-05 | Denial of Service | API Endpoints | mitigate | Rate limiting prevents abuse |
| T-03-05-06 | Elevation of Privilege | API Endpoints | mitigate | Input validation prevents injection |

**Critical Mitigations for This Plan:**
- Rate limiting prevents abuse
- Security headers via Helmet.js
- CORS configuration for production
- Input validation prevents injection
- Generic error messages prevent information leakage
- Proper HTTP status codes
</threat_model>

<verification>
- Rate limiting is configured and working
- Security headers are configured correctly
- CORS is configured correctly
- Input validation is implemented
- Error handling is implemented
- Test script validates all functionality
</verification>

<success_criteria>
- Rate limiting is complete
- Security headers are complete
- CORS is configured
- Input validation is implemented
- Error handling is implemented
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-search-api/03-05-SUMMARY.md`
</output>
