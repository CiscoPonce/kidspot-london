---
phase: 03-agentic-search-api
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/routes/search.js
autonomous: true
requirements:
  - AGENT-01
user_setup:
  - service: google_places_api
    why: "Google Places API for agentic search (full venue details)"
    env_vars:
      - name: GOOGLE_PLACES_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials → API Key"
  - service: osm_api
    why: "OpenStreetMap Overpass API for agentic search (full venue details)"
    env_vars: []

must_haves:
  truths:
    - "/api/search/venues/:id/details endpoint fetches full venue details on-demand"
    - "Full details are fetched from Google Places API for Google sources"
    - "Full details are fetched from OSM API for OSM sources"
    - "Full details include address, phone, website, reviews, photos, opening hours"
    - "Agentic search results are cached in Redis for 1 hour"
    - "Agentic search respects API rate limits"
  artifacts:
    - path: "backend/src/routes/search.js"
      provides: "Agentic search API routes"
      contains: "fetchGooglePlaceDetails(), fetchOSMDetails()"
  key_links:
    - from: "backend/src/routes/search.js"
      to: "Google Places API"
      via: "HTTP request"
      pattern: "axios.get('https://maps.googleapis.com/maps/api/place/details/json'"
    - from: "backend/src/routes/search.js"
      to: "OSM API"
      via: "HTTP request"
      pattern: "axios.get('https://overpass-api.de/api/interpreter'"
    - from: "backend/src/routes/search.js"
      to: "Redis cache"
      via: "cache key"
      pattern: "cache:venue:{id}:details"
---

<objective>
Implement agentic search for full venue details (address, phone, website, reviews, photos, opening hours) fetched on-demand from multiple sources.

Purpose: Fetch full venue details on-demand when users request them, rather than storing all details locally. This ensures data is always fresh and reduces storage costs.

Output: Working agentic search that fetches full venue details from Google Places and OSM APIs on-demand.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/03-agentic-search-api/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Google Places agentic search</name>
  <files>backend/src/routes/search.js</files>
  <action>Update backend/src/routes/search.js to add fetchGooglePlaceDetails function:
1. Function takes placeId parameter
2. Calls Google Places Details API
3. Requests fields: name, formatted_address, formatted_phone_number, website, rating, reviews, opening_hours, photos
4. Parses response and extracts venue details
5. Returns structured object with:
   - address
   - phone
   - website
   - rating
   - reviews
   - opening_hours
   - photos
6. Handles errors gracefully (API failures, rate limits)
7. Returns null if venue not found or error occurs

This function fetches full venue details from Google Places API on-demand.</action>
  <verify>fetchGooglePlaceDetails function returns correct venue details</verify>
  <done>Google Places agentic search implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement OSM agentic search</name>
  <files>backend/src/routes/search.js</files>
  <action>Update backend/src/routes/search.js to add fetchOSMDetails function:
1. Function takes osmId parameter
2. Calls Overpass API to get venue details
3. Queries for node with given ID
4. Parses response and extracts venue details
5. Returns structured object with:
   - address (from tags.address)
   - phone (from tags.phone)
   - website (from tags.website)
   - description (from tags.description)
6. Handles errors gracefully (API failures, timeouts)
7. Returns null if venue not found or error occurs

This function fetches full venue details from OSM API on-demand.</action>
  <verify>fetchOSMDetails function returns correct venue details</verify>
  <done>OSM agentic search implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement venue details endpoint</name>
  <files>backend/src/routes/search.js</files>
  <action>Update backend/src/routes/search.js to implement /api/search/venues/:id/details endpoint:
1. Gets venue ID from URL parameter
2. Queries database for basic venue info (name, type, lat, lon, source, source_id, sponsor_tier)
3. If venue not found, returns 404 error
4. Fetches full details from source based on venue.source:
   - If source is 'google', calls fetchGooglePlaceDetails
   - If source is 'osm', calls fetchOSMDetails
   - Otherwise, returns null for details
5. Returns combined response with:
   - basic: venue info from database
   - details: full details from source (or null)
6. Handles errors gracefully

This endpoint provides full venue details on-demand via agentic search.</action>
  <verify>/api/search/venues/:id/details endpoint returns combined basic + details</verify>
  <done>Venue details endpoint implemented</done>
</task>

<task type="auto">
  <name>Task 4: Test agentic search</name>
  <files>backend/scripts/test-agentic-search.js</files>
  <action>Create backend/scripts/test-agentic-search.js that:
1. Inserts test venue with Google Places source_id
2. Inserts test venue with OSM source_id
3. Tests /api/search/venues/:id/details for Google Places venue
4. Verifies that full details are fetched from Google Places API
5. Tests /api/search/venues/:id/details for OSM venue
6. Verifies that full details are fetched from OSM API
7. Verifies that basic info is included in response
8. Verifies that details are included in response
9. Reports test results

This test script validates that agentic search works correctly for both sources.</action>
  <verify>node backend/scripts/test-agentic-search.js passes all tests</verify>
  <done>Agentic search tested for both sources</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agentic Search → Google Places API | On-demand venue details |
| Agentic Search → OSM API | On-demand venue details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | Google Places API | mitigate | Validate API responses, use official API |
| T-03-02-02 | Tampering | Agentic Search Results | mitigate | Validate all data before returning |
| T-03-02-03 | Information Disclosure | Venue Details | accept | Venue details are public information |
| T-03-02-04 | Denial of Service | Google Places API | mitigate | Respect rate limits, handle failures gracefully |
| T-03-02-05 | Denial of Service | OSM API | mitigate | Respect rate limits, handle failures gracefully |

**Critical Mitigations for This Plan:**
- Validate API responses before returning data
- Respect API rate limits (Google Places, OSM)
- Handle API failures gracefully (return null, log error)
- Cache results to reduce API calls
- Return null for missing venues rather than errors
</threat_model>

<verification>
- fetchGooglePlaceDetails function is implemented
- fetchOSMDetails function is implemented
- /api/search/venues/:id/details endpoint is implemented
- Google Places agentic search works correctly
- OSM agentic search works correctly
- Basic info is included in response
- Full details are included in response
- API failures are handled gracefully
- Test script validates all functionality
</verification>

<success_criteria>
- fetchGooglePlaceDetails function is complete
- fetchOSMDetails function is complete
- /api/search/venues/:id/details endpoint is complete
- Google Places agentic search works correctly
- OSM agentic search works correctly
- Full details are fetched on-demand
- API failures are handled gracefully
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-search-api/03-02-SUMMARY.md`
</output>
