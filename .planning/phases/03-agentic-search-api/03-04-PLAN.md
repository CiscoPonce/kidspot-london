---
phase: 03-agentic-search-api
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
files_modified:
  - backend/src/routes/search.js
  - backend/src/routes/sponsors.js
autonomous: true
requirements:
  - API-02
  - CACHE-01
user_setup: []
must_haves:
  truths:
    - "Search results are cached in Redis for 1 hour"
    - "Venue details are cached in Redis for 1 hour"
    - "Geocoding results are cached in Redis for 30 days"
    - "Cache keys are structured (search:{lat}:{lon}:{radius}:{type})"
    - "Cache hits reduce database load"
    - "Cache TTL is configurable"
    - "Cache is invalidated on updates"
  artifacts:
    - path: "backend/src/routes/search.js"
      provides: "Search API with Redis caching"
      contains: "redis.get(), redis.set(), cache key structure"
    - path: "backend/src/routes/sponsors.js"
      provides: "Sponsor API with Redis caching"
      contains: "redis.get(), redis.set(), cache key structure"
  key_links:
    - from: "backend/src/routes/search.js"
      to: "Redis cache"
      via: "cache key"
      pattern: "redis.get('search:{lat}:{lon}:{radius}:{type}')"
    - from: "backend/src/routes/search.js"
      to: "Redis cache"
      via: "cache key"
      pattern: "redis.set('search:{lat}:{lon}:{radius}:{type}', JSON.stringify(data), 'EX', 3600)"
---

<objective>
Implement Redis caching layer for search results and venue details to reduce database load and improve response times.

Purpose: Cache search results and venue details in Redis to reduce database load, improve response times, and respect API rate limits. Cache keys are structured for easy invalidation.

Output: Working Redis caching layer with configurable TTL and structured cache keys.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/03-agentic-search-api/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement search result caching</name>
  <files>backend/src/routes/search.js</files>
  <action>Update backend/src/routes/search.js to add Redis caching:
1. Import Redis client: const redis = require('ioredis')
2. Create Redis client: const redis = new Redis(process.env.REDIS_URL)
3. In /api/search/venues endpoint:
   - Generate cache key: `search:${lat}:${lon}:${radius_miles}:${type}`
   - Check cache: const cached = await redis.get(cacheKey)
   - If cached, return cached results
   - If not cached, query database
   - Cache results: await redis.set(cacheKey, JSON.stringify(response), 'EX', 3600)
   - Return results
4. In /api/search/venues/:id/details endpoint:
   - Generate cache key: `venue:${id}:details`
   - Check cache: const cached = await redis.get(cacheKey)
   - If cached, return cached results
   - If not cached, fetch from source
   - Cache results: await redis.set(cacheKey, JSON.stringify(response), 'EX', 3600)
   - Return results

Cache configuration:
- Search results TTL: 1 hour (3600 seconds)
- Venue details TTL: 1 hour (3600 seconds)
- Cache key structure: {type}:{id}:{params}</action>
  <verify>Search results are cached and retrieved correctly</verify>
  <done>Search result caching implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement sponsor statistics caching</name>
  <files>backend/src/routes/sponsors.js</files>
  <action>Update backend/src/routes/sponsors.js to add Redis caching:
1. Import Redis client: const redis = require('ioredis')
2. Create Redis client: const redis = new Redis(process.env.REDIS_URL)
3. In /api/sponsors/stats endpoint:
   - Generate cache key: `sponsors:stats`
   - Check cache: const cached = await redis.get(cacheKey)
   - If cached, return cached results
   - If not cached, query database
   - Cache results: await redis.set(cacheKey, JSON.stringify(response), 'EX', 300)
   - Return results
4. In /api/sponsors/pricing endpoint:
   - Generate cache key: `sponsors:pricing`
   - Check cache: const cached = await redis.get(cacheKey)
   - If cached, return cached results
   - If not cached, return static pricing
   - Cache results: await redis.set(cacheKey, JSON.stringify(response), 'EX', 86400)
   - Return results

Cache configuration:
- Sponsor stats TTL: 5 minutes (300 seconds)
- Sponsor pricing TTL: 24 hours (86400 seconds)</action>
  <verify>Sponsor statistics are cached and retrieved correctly</verify>
  <done>Sponsor statistics caching implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement cache invalidation</name>
  <files>backend/src/routes/sponsors.js</files>
  <action>Update backend/src/routes/sponsors.js to add cache invalidation:
1. In /api/sponsors/venues/:id/tier endpoint:
   - After updating sponsor tier, invalidate relevant caches
   - Delete search cache keys (all search results)
   - Delete sponsor stats cache
   - Use redis.del with pattern matching
2. In /api/sponsors/venues/bulk/tier endpoint:
   - After bulk updating, invalidate relevant caches
   - Delete search cache keys (all search results)
   - Delete sponsor stats cache

Cache invalidation strategy:
- Invalidate all search caches when sponsor tiers change
- Invalidate sponsor stats cache when sponsor tiers change
- Use pattern matching to delete multiple keys</action>
  <verify>Cache invalidation works correctly</verify>
  <done>Cache invalidation implemented</done>
</task>

<task type="auto">
  <name>Task 4: Test caching</name>
  <files>backend/scripts/test-caching.js</files>
  <action>Create backend/scripts/test-caching.js that:
1. Inserts test venues
2. Tests search result caching:
   - First call queries database
   - Second call returns cached results
   - Verifies cache hit
3. Tests venue details caching:
   - First call fetches from source
   - Second call returns cached results
   - Verifies cache hit
4. Tests sponsor statistics caching:
   - First call queries database
   - Second call returns cached results
   - Verifies cache hit
5. Tests cache invalidation:
   - Updates sponsor tier
   - Verifies that search cache is invalidated
   - Verifies that sponsor stats cache is invalidated
6. Tests cache TTL:
   - Sets cache with short TTL
   - Waits for TTL to expire
   - Verifies that cache is expired
7. Reports test results

This test script validates that caching works correctly and improves performance.</action>
  <verify>node backend/scripts/test-caching.js passes all tests</verify>
  <done>Caching tested with cache hits and invalidation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Redis Cache | Caching layer |
| Cache Invalidation → Redis Cache | Cache updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Cache Data | mitigate | Cache is ephemeral, data is source of truth |
| T-03-04-02 | Information Disclosure | Cache Data | accept | Cached data is public information |
| T-03-04-03 | Denial of Service | Redis Cache | mitigate | Cache has TTL, prevents stale data |

**Critical Mitigations for This Plan:**
- Cache is ephemeral, database is source of truth
- Cache TTL prevents stale data
- Cache invalidation on updates
- Structured cache keys for easy invalidation
- Cache reduces database load
</threat_model>

<verification>
- Search results are cached correctly
- Venue details are cached correctly
- Sponsor statistics are cached correctly
- Cache hits reduce database load
- Cache TTL is respected
- Cache invalidation works correctly
- Test script validates all functionality
</verification>

<success_criteria>
- Search result caching is complete
- Venue details caching is complete
- Sponsor statistics caching is complete
- Cache hits reduce database load
- Cache TTL is respected
- Cache invalidation works correctly
- Test script validates all functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-search-api/03-04-SUMMARY.md`
</output>
