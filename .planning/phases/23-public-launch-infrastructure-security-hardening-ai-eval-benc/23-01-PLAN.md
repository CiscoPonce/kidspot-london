---
phase: 23
plan: 1
wave: 1
depends_on: []
files_modified:
  - scripts/caddy/Caddyfile
  - docker-compose.prod.yml
---

# Plan 23-01: SSL Reverse Proxy & Domain Mapping Readiness

<task>
  <objective>Configure Caddyfile for automatic SSL generation for the main domain and API subdomain.</objective>
  <action>Create or update scripts/caddy/Caddyfile with proxy routes for kidspot.london mapping to the frontend, and api.kidspot.london mapping to the backend, enabling Let's Encrypt / ZeroSSL HTTPS.</action>
  <verify>Ensure Caddyfile syntax is valid and proxies to the correct internal container ports.</verify>
  <done>false</done>
</task>

<task>
  <objective>Setup Caddy service in the production docker-compose.</objective>
  <action>Add a Caddy service entry to docker-compose.prod.yml, exposing ports 80 and 443, mounting the Caddyfile, and mapping persistent storage for SSL certificates.</action>
  <verify>Caddy service is present in docker-compose.prod.yml with correct volumes and ports.</verify>
  <done>false</done>
</task>

<task>
  <objective>Configure CORS_ORIGIN environment variable setup.</objective>
  <action>Update environment configuration instructions to lock down CORS_ORIGIN to https://kidspot.london and set NEXT_PUBLIC_API_URL to https://api.kidspot.london/api.</action>
  <verify>Environment variable instructions are updated to restrict CORS and point to the production API.</verify>
  <done>false</done>
</task>
