---
phase: 01-data-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
gap_closure: true
autonomous: true
requirements:
  - INFRA-01
user_setup: []

must_haves:
  truths:
    - "Worker service is defined in docker-compose.yml"
    - "Worker service builds from backend/Dockerfile.worker"
    - "Worker service depends on postgres and redis with healthcheck conditions"
    - "backend/Dockerfile.worker is no longer orphaned"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Docker Compose orchestration including worker service"
      contains: "services: postgres, redis, api, worker"
    - path: "backend/Dockerfile.worker"
      provides: "Worker service container image"
      contains: "FROM node:20"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/Dockerfile.worker"
      via: "build context"
      pattern: "build: ./backend, dockerfile: Dockerfile.worker"
---

<objective>
Fix gap G-01: Worker service is not defined in docker-compose.yml. The backend/Dockerfile.worker exists but is not referenced, making it orphaned.

Purpose: Add the worker service to docker-compose.yml so all 4 planned services (postgres, redis, api, worker) are properly orchestrated.

Output: docker-compose.yml with worker service definition, Dockerfile.worker properly linked.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docker-compose.yml
@backend/Dockerfile.worker
@.planning/phases/01-data-foundation/01-01-PLAN.md
@.planning/phases/01-data-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add worker service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>Add worker service definition to docker-compose.yml after the api service:

```yaml
  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile.worker
    environment:
      - DATABASE_URL=postgres://kidspot_admin:${DB_PASSWORD}@postgres:5432/kidspot
      - REDIS_URL=redis://redis:6379
      - NODE_ENV=production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
```

The worker service mirrors the api service's environment configuration but uses Dockerfile.worker. It depends on postgres and redis with healthcheck conditions to ensure the database and cache are ready before the worker starts.</action>
  <verify>docker-compose config validates without errors</verify>
  <done>Worker service added to docker-compose.yml with proper build context and dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify worker service can start</name>
  <files>docker-compose.yml</files>
  <behavior>
    - Test 1: docker-compose config produces valid output with 4 services
    - Test 2: Worker service definition contains build context pointing to backend/Dockerfile.worker
    - Test 3: Worker service depends_on postgres and redis with condition: service_healthy
  </behavior>
  <action>Run docker-compose config to validate the complete configuration includes all 4 services (postgres, redis, api, worker).

Verify the worker service has:
- build context: ./backend
- dockerfile: Dockerfile.worker
- depends_on with healthcheck conditions for postgres and redis
- environment variables for DATABASE_URL and REDIS_URL
- restart: unless-stopped
- healthcheck defined</action>
  <verify>docker-compose config outputs all 4 services without errors</verify>
  <done>Worker service properly defined and validated</done>
</task>

</tasks>

<verification>
- docker-compose config validates without errors
- docker-compose.yml contains worker service definition
- backend/Dockerfile.worker is referenced by docker-compose.yml
- Worker service has proper dependencies on postgres and redis
</verification>

<success_criteria>
- docker-compose.yml defines all 4 services: postgres, redis, api, worker
- Worker service builds from backend/Dockerfile.worker
- Worker service depends on postgres and redis with healthcheck conditions
- backend/Dockerfile.worker is no longer orphaned (properly linked)
- Configuration passes docker-compose config validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-04-SUMMARY.md`
</output>
