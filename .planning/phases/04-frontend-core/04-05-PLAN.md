---
phase: 04-frontend-core
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
files_modified:
  - frontend/src/components/modals/venue-detail-modal.tsx
  - frontend/src/components/map/venue-map-snippet.tsx
  - frontend/src/app/page.tsx
autonomous: false
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - "Venue detail modal opens when venue is selected"
    - "Modal shows venue name, type, address, phone"
    - "Modal shows website button (opens in new tab)"
    - "Modal shows call button (tel: link)"
    - "Modal shows small map snippet with venue pin"
    - "Modal has close button and closes on backdrop tap"
    - "Modal shows loading state while fetching details"
    - "Modal shows error state if details unavailable"
  artifacts:
    - path: "frontend/src/components/modals/venue-detail-modal.tsx"
      provides: "Venue detail modal with call/website buttons"
      contains: "VenueDetailModal component"
    - path: "frontend/src/components/map/venue-map-snippet.tsx"
      provides: "Small map showing single venue location"
      contains: "VenueMapSnippet component"
  key_links:
    - from: "frontend/src/components/modals/venue-detail-modal.tsx"
      to: "getVenueDetails API"
      via: "useQuery"
      pattern: "useQuery.*getVenueDetails"
    - from: "frontend/src/components/modals/venue-detail-modal.tsx"
      to: "VenueMapSnippet"
      via: "import"
      pattern: "VenueMapSnippet"
    - from: "frontend/src/components/modals/venue-detail-modal.tsx"
      to: "Phone/Website"
      via: "tel:/https: links"
      pattern: "tel:|https://"
---

<objective>
Implement venue detail modal with map snippet, call button, and website button.

Purpose: When a user selects a venue, show a modal with full details including contact buttons and a small map snippet showing the venue location.

Output: Working venue detail modal with map snippet and contact buttons.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/04-frontend-core/04-01-PLAN.md
@.planning/phases/04-frontend-core/04-03-PLAN.md
@.planning/phases/04-frontend-core/04-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create venue map snippet component</name>
  <files>frontend/src/components/map/venue-map-snippet.tsx</files>
  <action>Create compact map snippet showing single venue:
1. Create frontend/src/components/map/venue-map-snippet.tsx:
   - Props: { lat: number, lon: number, name: string, zoom?: number }
   - Small static map (no clustering needed)
   - Shows single venue pin
   - Centers on venue location
   - Uses same MapLibre GL JS as main map
   - Fixed height: 150px
   - Non-interactive (no pan/zoom controls)

2. Implementation:
   - Lightweight MapLibre instance
   - Single marker at venue location
   - Basic navigationControl disabled
   - Attribution hidden or minimal
   - No popup (name shown in modal header instead)

This provides location context in the detail modal.</action>
  <verify>Map snippet renders with single venue pin centered</verify>
  <done>Venue map snippet component created</done>
</task>

<task type="auto">
  <name>Task 2: Create venue detail modal</name>
  <files>frontend/src/components/modals/venue-detail-modal.tsx</files>
  <action>Create venue detail modal component:
1. Create frontend/src/components/modals/venue-detail-modal.tsx:
   - Props: { venue: Venue, isOpen: boolean, onClose: () => void }
   - Uses React Portal for modal rendering
   - Backdrop with blur effect
   - Slides up from bottom on mobile (sheet style)
   - Centers on desktop

2. Content sections:
   - Header: venue name, type badge, sponsor tier badge
   - Map snippet: VenueMapSnippet component
   - Details section: address, distance
   - Actions section: Call button, Website button
   - Loading state while fetching details via getVenueDetails
   - Error state if API call fails

3. Action buttons:
   - Call button: <a href="tel:{phone}"> with phone number
   - Website button: <a href="{website}" target="_blank" rel="noopener">
   - Only show buttons if data available

4. Close behavior:
   - X button in header
   - Click backdrop to close
   - Escape key to close
   - Swipe down on mobile to close

This is the main detail view for individual venues.</action>
  <verify>Modal opens with venue details, shows call/website buttons when available</verify>
  <done>Venue detail modal created</done>
</task>

<task type="auto">
  <name>Task 3: Wire up modal with venue selection</name>
  <files>frontend/src/app/page.tsx</files>
  <action>Update main page to wire up modal:
1. Update frontend/src/app/page.tsx:
   - Import VenueDetailModal component
   - Add state: selectedVenue (from list or map click)
   - When venue selected in list or map → open modal
   - Pass venue to modal
   - Handle onClose → clear selectedVenue

2. Integration points:
   - VenueList onVenueSelect → sets selectedVenue
   - VenueMap onVenueClick → sets selectedVenue
   - Modal onClose → clears selectedVenue

3. UX considerations:
   - Smooth animation when modal opens
   - Proper focus management for accessibility
   - Body scroll lock when modal open

This connects the modal to the rest of the app.</action>
  <verify>Modal opens when venue selected, closes on X/backdrop/escape</verify>
  <done>Modal wired up with venue selection</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify venue detail flow</name>
  <what-built>Complete venue detail flow: list → modal → call/website actions</what-built>
  <how-to-verify>
1. Run the Next.js dev server (cd frontend && npm run dev)
2. Open http://localhost:3000 on mobile emulator or browser
3. Click "Use My Location" or enter a postcode
4. Verify venues appear in list and on map
5. Tap/click a venue in the list
6. Verify modal opens with:
   - Venue name and type
   - Small map showing venue location
   - Call button (if phone available)
   - Website button (if website available)
7. Tap X, backdrop, or swipe down to close modal
8. Click a pin on the map
9. Verify modal opens from map click too
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Fetching venue details |
| Browser → External Websites | Website button opens external site |
| Browser → Phone App | Call button triggers phone dialer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | Venue Details | accept | Venue details are public info |
| T-04-05-02 | Spoofing | External Links | mitigate | Validate URL format, use rel="noopener" |
| T-04-05-03 | Denial of Service | Detail API | accept | Graceful error state shown |
| T-04-05-04 | Elevation of Privilege | Phone Dialer | accept | User initiates call action |

**Critical Mitigations for This Plan:**
- Website links use rel="noopener" to prevent tab hijacking
- Phone numbers validated before tel: link
- Error states shown for API failures
- No sensitive data exposed
</threat_model>

<verification>
- Venue detail modal opens when venue selected
- Modal shows venue name, type, address
- Call button (tel: link) shown when phone available
- Website button opens in new tab when website available
- Map snippet shows venue location
- Modal closes on X, backdrop tap, Escape, swipe
</verification>

<success_criteria>
- Detail modal shows venue info with call/website buttons
- Map snippet shows venue pin location
- Modal is mobile-friendly (sheet style on mobile)
- Call button uses tel: link format
- Website button opens in new tab
- Loading and error states work correctly
- Modal closes correctly via multiple methods
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-core/04-05-SUMMARY.md`
</output>
