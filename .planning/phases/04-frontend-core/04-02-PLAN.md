---
phase: 04-frontend-core
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/components/search/search-bar.tsx
  - frontend/src/components/search/location-button.tsx
  - frontend/src/components/search/radius-slider.tsx
  - frontend/src/hooks/use-location.ts
  - frontend/src/hooks/use-search.ts
autonomous: true
requirements:
  - UI-02
user_setup: []

must_haves:
  truths:
    - "Search bar accepts postcode text input"
    - "'Use My Location' button requests geolocation permission"
    - "Radius slider adjusts search area from 1 to 10 miles"
    - "Radius slider shows current value"
    - "Search triggers on location change or radius change"
    - "Geolocation API is used for browser location detection"
    - "Postcode is geocoded via Photon API before search"
  artifacts:
    - path: "frontend/src/components/search/search-bar.tsx"
      provides: "Combined search bar with postcode input and location button"
      contains: "SearchBar component"
    - path: "frontend/src/components/search/radius-slider.tsx"
      provides: "Radius slider control (1-10 miles)"
      contains: "RadiusSlider component"
    - path: "frontend/src/hooks/use-location.ts"
      provides: "Geolocation hook for browser location"
      contains: "useCurrentPosition, geocodePostcode"
    - path: "frontend/src/hooks/use-search.ts"
      provides: "Search state management"
      contains: "useSearch hook with lat, lon, radius"
  key_links:
    - from: "frontend/src/components/search/search-bar.tsx"
      to: "use-search hook"
      via: "React Query mutation"
      pattern: "useSearch\\.mutate"
    - from: "frontend/src/hooks/use-location.ts"
      to: "Geolocation API"
      via: "navigator.geolocation.getCurrentPosition"
      pattern: "navigator.geolocation"
    - from: "frontend/src/hooks/use-location.ts"
      to: "Photon API"
      via: "fetch"
      pattern: "photon.komoot.io"
---

<objective>
Create search bar with postcode input, "Use My Location" button, and radius slider (1-10 miles).

Purpose: Allow users to search for venues by either typing a postcode or using their current location, with adjustable search radius.

Output: Working search bar component with location detection and radius slider.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/04-frontend-core/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create location hook</name>
  <files>frontend/src/hooks/use-location.ts</files>
  <action>Create frontend/src/hooks/use-location.ts with:
1. useCurrentPosition hook:
   - Uses navigator.geolocation.getCurrentPosition
   - Returns { lat, lon } or null if denied/unavailable
   - Handles permission states (granted, denied, prompt)
   - Loading and error states

2. geocodePostcode function:
   - Takes postcode string
   - Calls Photon API (https://photon.komoot.io)
   - Returns { lat, lon } for first result in London area
   - Handles no results and API errors

3. Types exported:
   - CurrentPosition state
   - GeocodeResult

The Photon API is free and doesn't require an API key.</action>
  <verify>useCurrentPosition returns coordinates when permission granted, geocodePostcode returns coordinates for valid postcode</verify>
  <done>Location hook created with geolocation and postcode geocoding</done>
</task>

<task type="auto">
  <name>Task 2: Create radius slider component</name>
  <files>frontend/src/components/search/radius-slider.tsx</files>
  <action>Create frontend/src/components/search/radius-slider.tsx:
1. Props: { value: number, onChange: (value: number) => void }
2. Range input styled for mobile touch:
   - min: 1, max: 10, step: 0.5
   - Custom styled track and thumb
   - Shows current value as label
3. Display: "Within X mile(s)" text
4. Mobile-friendly touch target (min 44px)
5. TailwindCSS styling with primary color for track fill

The slider should feel native on mobile devices.</action>
  <verify>Radius slider renders with correct min/max (1-10 miles) and updates on change</verify>
  <done>Radius slider component created</done>
</task>

<task type="auto">
  <name>Task 3: Create location button component</name>
  <files>frontend/src/components/search/location-button.tsx</files>
  <action>Create frontend/src/components/search/location-button.tsx:
1. Props: { onLocationReceived: (lat: number, lon: number) => void }
2. "Use My Location" button:
   - Icon: location/dot icon (Lucide or similar)
   - Loading state with spinner when requesting
   - Error state if permission denied
   - Success state briefly shown after click
3. Handles:
   - Permission granted → call onLocationReceived with coords
   - Permission denied → show error message
   - Geolocation unavailable → show unavailable message
4. Mobile-friendly (full width on mobile)

The button triggers browser geolocation prompt.</action>
  <verify>Location button shows loading state during geolocation request</verify>
  <done>Location button component created</done>
</task>

<task type="auto">
  <name>Task 4: Create search bar and use-search hook</name>
  <files>frontend/src/components/search/search-bar.tsx, frontend/src/hooks/use-search.ts</files>
  <action>Create search bar and search state hook:
1. frontend/src/hooks/use-search.ts:
   - useSearch hook with React Context
   - State: { lat, lon, radius, postcode }
   - setSearchLocation(lat, lon) function
   - setRadius(radius) function
   - Context provider for sharing search state

2. frontend/src/components/search/search-bar.tsx:
   - SearchBar component combining all parts
   - Text input for postcode (placeholder: "Enter postcode")
   - "Use My Location" button
   - Radius slider
   - Search icon button
   - On submit: geocode postcode if text input used, or use current location
   - Calls setSearchLocation with results
   - Mobile-first layout (stacks vertically on mobile)

3. Include debounce for postcode input (500ms)</action>
  <verify>Search bar accepts postcode, detects location, and updates search state</verify>
  <done>Search bar and use-search hook created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Geolocation API | User location permission request |
| Frontend → Photon API | Postcode geocoding (no API key needed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Geolocation | accept | User explicitly grants permission |
| T-04-02-02 | Denial of Service | Photon API | accept | Free API with rate limits |
| T-04-02-03 | Spoofing | Postcode Input | mitigate | Sanitize input, validate format |

**Critical Mitigations for This Plan:**
- Geolocation requires explicit user permission
- Postcode input sanitized before API call
- Error states handled gracefully for all failure modes
</threat_model>

<verification>
- Search bar accepts postcode text input
- "Use My Location" button requests browser geolocation
- Radius slider adjusts from 1-10 miles
- Search state updates when location or radius changes
- Geolocation API is used for browser location detection
- Postcode is geocoded via Photon API
</verification>

<success_criteria>
- Search bar accepts postcode or "Use My Location"
- Radius slider adjusts search area (1-10 miles)
- Geolocation works on mobile and desktop
- Postcode geocoding returns London coordinates
- Search state is managed via React Context
- All components are mobile-first responsive
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-core/04-02-SUMMARY.md`
</output>
