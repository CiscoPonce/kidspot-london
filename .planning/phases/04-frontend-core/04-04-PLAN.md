---
phase: 04-frontend-core
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/components/venues/venue-list.tsx
  - frontend/src/components/venues/venue-card.tsx
  - frontend/src/components/venues/venue-list-item.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - "Venue list displays below search bar"
    - "Venues are sorted by distance (nearest first)"
    - "Each venue shows name, type, distance"
    - "Sponsor venues show tier badge (gold, silver, bronze)"
    - "Tapping a venue card highlights it"
    - "List is scrollable with smooth scrolling"
    - "Empty state shows when no venues found"
    - "Loading state shows while fetching venues"
  artifacts:
    - path: "frontend/src/components/venues/venue-list.tsx"
      provides: "Container for venue list with sorting"
      contains: "VenueList component"
    - path: "frontend/src/components/venues/venue-card.tsx"
      provides: "Individual venue card with distance"
      contains: "VenueCard component"
    - path: "frontend/src/components/venues/venue-list-item.tsx"
      provides: "Venue list row item"
      contains: "VenueListItem component"
  key_links:
    - from: "frontend/src/components/venues/venue-list.tsx"
      to: "use-search hook"
      via: "React Context"
      pattern: "useSearchContext"
    - from: "frontend/src/components/venues/venue-list.tsx"
      to: "fetchVenues API"
      via: "useQuery"
      pattern: "useQuery.*fetchVenues"
    - from: "frontend/src/components/venues/venue-card.tsx"
      to: "venue detail modal"
      via: "onClick"
      pattern: "onSelect.*Venue"
---

<objective>
Build venue list view with distance sorting for the KidSpot London frontend.

Purpose: Display search results as a scrollable list sorted by distance, with sponsor badges and mobile-friendly touch targets.

Output: Working venue list component with distance-based sorting.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/04-frontend-core/04-01-PLAN.md
@.planning/phases/04-frontend-core/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create venue card component</name>
  <files>frontend/src/components/venues/venue-card.tsx</files>
  <action>Create venue card component:
1. Create frontend/src/components/venues/venue-card.tsx:
   - Props: { venue: Venue, distance: number, onSelect: () => void, isSelected?: boolean }
   - Display: venue name, type icon, distance in miles
   - Sponsor badge if applicable (gold/silver/bronze)
   - Type icon (park, community hall, softplay, etc.)
   - Selected state styling
   - onClick handler for onSelect
   - Mobile-friendly touch target (min 44px height)

2. Styling:
   - Card with shadow and rounded corners
   - Hover/press state on mobile
   - Sponsor tier badge with appropriate color
   - Distance displayed as "0.3 miles" format
   - Type displayed with appropriate icon

3. Type icons (use Lucide or similar):
   - park → Trees icon
   - community_hall → Building icon
   - softplay → Joystick icon
   - sports_centre → Dumbbell icon
   - other → MapPin icon</action>
  <verify>VenueCard renders with name, distance, and sponsor badge</verify>
  <done>Venue card component created</done>
</task>

<task type="auto">
  <name>Task 2: Create venue list component</name>
  <files>frontend/src/components/venues/venue-list.tsx</files>
  <action>Create venue list component:
1. Create frontend/src/components/venues/venue-list.tsx:
   - Props: { venues: Venue[], onVenueSelect: (venue: Venue) => void, selectedId?: number }
   - Fetches venues using useSearchContext (lat, lon, radius)
   - Uses useQuery with fetchVenues from API
   - Sorts venues by distance (nearest first)
   - Renders VenueCard for each venue
   - Scrollable container with smooth scroll
   - Pull-to-refresh on mobile

2. States:
   - Loading: skeleton cards or spinner
   - Empty: "No venues found in this area" message
   - Error: error message with retry button
   - Loaded: list of venue cards

3. Layout:
   - Full width, scrollable
   - On mobile: list above map (or below based on UX decision)
   - On desktop: list in sidebar or below map

4. Performance:
   - Virtualized list for large result sets (react-window if needed)
   - Debounced search on radius/location change</action>
  <verify>VenueList renders venues sorted by distance with loading and empty states</verify>
  <done>Venue list component created</done>
</task>

<task type="auto">
  <name>Task 3: Create list-item component for compact view</name>
  <files>frontend/src/components/venues/venue-list-item.tsx</files>
  <action>Create compact venue list item:
1. Create frontend/src/components/venues/venue-list-item.tsx:
   - Props: { venue: Venue, distance: number, onTap: () => void }
   - Compact horizontal layout: icon | name + type | distance
   - Used for mobile bottom sheet or compact list
   - Tap target for entire row
   - Highlight on tap

2. Layout:
   - Height: 56px
   - Icon on left (32x32)
   - Name and type in middle (truncate long names)
   - Distance on right

This provides a compact view for mobile bottom sheets.</action>
  <verify>VenueListItem renders as compact horizontal row</verify>
  <done>List item component created</done>
</task>

<task type="auto">
  <name>Task 4: Integrate list into main page</name>
  <files>frontend/src/app/page.tsx</files>
  <action>Update main page to include venue list:
1. Update frontend/src/app/page.tsx:
   - Import VenueList component
   - Layout: SearchBar at top, VenueList below
   - VenueList shows venues from search results
   - onVenueSelect highlights venue on map

2. Mobile-first layout:
   - Search bar: fixed at top
   - Venue list: scrollable below search
   - Map: can be toggled visible/hidden (for Phase 05)

3. Coordinate with map plan (04-03):
   - List and map share venue data
   - Selecting venue in list highlights on map
   - Map marker click scrolls to list item

This integrates the venue list into the main app flow.</action>
  <verify>Venue list appears on main page, shows venues from search</verify>
  <done>Venue list integrated into main page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Fetching venue search results |
| Frontend State → UI | Displaying venue data to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Venue Data | accept | Venue data is public information |
| T-04-04-02 | Denial of Service | Search API | mitigate | Rate limiting at API level |
| T-04-04-03 | Spoofing | Venue Names | accept | Display only, no user input |

**Critical Mitigations for This Plan:**
- Venue data from validated API responses
- No sensitive data displayed
- Loading states prevent information disclosure
</threat_model>

<verification>
- Venue list displays below search bar
- Venues are sorted by distance (nearest first)
- Sponsor venues show tier badge
- Tapping a venue card highlights it
- Empty state shows when no venues found
- Loading state shows while fetching venues
</verification>

<success_criteria>
- List view shows venues sorted by distance
- Each venue shows name, type, distance
- Sponsor badges display correctly (gold, silver, bronze)
- Loading and empty states work correctly
- List is scrollable on mobile
- Tapping a venue provides visual feedback
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-core/04-04-SUMMARY.md`
</output>
