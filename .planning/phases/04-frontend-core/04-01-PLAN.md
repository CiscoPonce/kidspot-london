---
phase: 04-frontend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tailwind.config.js
  - frontend/next.config.js
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/lib/api.ts
  - frontend/src/providers/query-provider.tsx
autonomous: true
requirements:
  - UI-01
  - STATE-01
user_setup: []

must_haves:
  truths:
    - "Next.js app runs on http://localhost:3000"
    - "TailwindCSS is configured with mobile-first breakpoints"
    - "React Query provider wraps the application"
    - "API client is configured to call backend at localhost:4000"
    - "App renders on mobile viewport (375px) and desktop (1280px)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Next.js 14 with React 18, TailwindCSS, React Query"
      contains: "next@14, react@18, tailwindcss, @tanstack/react-query"
    - path: "frontend/src/app/page.tsx"
      provides: "Main page component"
      contains: "export default function Page"
    - path: "frontend/src/providers/query-provider.tsx"
      provides: "React Query provider"
      contains: "QueryClientProvider"
    - path: "frontend/src/lib/api.ts"
      provides: "API client for backend communication"
      contains: "fetchVenues, getVenueDetails"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "QueryClientProvider"
      via: "import"
      pattern: "QueryProvider"
    - from: "frontend/src/lib/api.ts"
      to: "localhost:4000"
      via: "fetch"
      pattern: "localhost:4000/api"
---

<objective>
Setup Next.js 14 project with TailwindCSS and React Query for the KidSpot London frontend.

Purpose: Establish the frontend foundation with mobile-first responsive design, state management via React Query, and API client configuration for backend communication.

Output: Working Next.js application with TailwindCSS styling and React Query provider.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project</name>
  <files>frontend/package.json, frontend/next.config.js, frontend/tsconfig.json</files>
  <action>Initialize frontend project with:
1. Create frontend/package.json with:
   - next@14, react@18, react-dom@18
   - typescript, @types/react, @types/node
   - tailwindcss, postcss, autoprefixer
   - @tanstack/react-query@5
   - maplibre-gl (MapLibre GL JS for maps)
   - Use exact versions for consistency

2. Create next.config.js:
   - Set output: 'standalone' for Docker
   - Configure images domain for venue photos
   - Enable strict mode

3. Create tsconfig.json with path aliases (@/* → ./src/*)

4. Run npm install in frontend directory</action>
  <verify>npm install succeeds in frontend directory</verify>
  <done>Next.js project initialized with dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Configure TailwindCSS</name>
  <files>frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/app/globals.css</files>
  <action>Configure TailwindCSS with mobile-first design:
1. Create tailwind.config.js with:
   - content: ['./src/**/*.{js,ts,jsx,tsx}']
   - theme.extend with custom colors (primary: blue, secondary: slate)
   - Mobile-first breakpoints: sm:640px, md:768px, lg:1024px
   - Custom spacing scale
   - Font family configuration

2. Create postcss.config.js:
   - tailwindcss, autoprefixer plugins

3. Create frontend/src/app/globals.css:
   - @tailwind base, components, utilities
   - Custom CSS variables for theming
   - Base styles for html, body
   - Custom scrollbar styles
   - Mobile touch action improvements</action>
  <verify>TailwindCSS builds without errors</verify>
  <done>TailwindCSS configured with mobile-first breakpoints</done>
</task>

<task type="auto">
  <name>Task 3: Create React Query provider and API client</name>
  <files>frontend/src/providers/query-provider.tsx, frontend/src/lib/api.ts</files>
  <action>Create React Query provider and API client:
1. Create frontend/src/providers/query-provider.tsx:
   - Export QueryProvider component
   - Wraps app with QueryClientProvider
   - Configure QueryClient with:
     - staleTime: 5 minutes
     - gcTime: 10 minutes
     - retry: 1
     - refetchOnWindowFocus: false

2. Create frontend/src/lib/api.ts:
   - Base fetch function with error handling
   - Types for Venue, VenueSearchParams, VenueDetails
   - fetchVenues(lat, lon, radiusMiles, type?) function
   - getVenueDetails(venueId) function
   - Configured to call http://localhost:4000/api
   - Returns typed responses</action>
  <verify>API client functions are properly typed and exported</verify>
  <done>React Query provider and API client created</done>
</task>

<task type="auto">
  <name>Task 4: Create app layout and main page</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/page.tsx</files>
  <action>Create the main app structure:
1. Create frontend/src/app/layout.tsx:
   - Import globals.css
   - Wrap with QueryProvider
   - Add html lang="en" and mobile viewport meta
   - Include proper metadata (title: "KidSpot London")

2. Create frontend/src/app/page.tsx:
   - Simple placeholder that renders "KidSpot London" heading
   - Include MobileWidth and DesktopWidth indicators for testing
   - Export as default

This creates the shell that subsequent plans will populate.</action>
  <verify>Next.js dev server starts and page renders at http://localhost:3000</verify>
  <done>App layout and main page created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | HTTP requests to localhost:4000/api |
| Browser → Next.js Server | Static and dynamic content serving |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | API Client | mitigate | Validate env vars, don't expose API keys |
| T-04-01-02 | Denial of Service | Next.js Server | mitigate | Rate limiting at API level |
| T-04-01-03 | Information Disclosure | Error Messages | mitigate | Don't expose internal errors to client |

**Critical Mitigations for This Plan:**
- API client configured for internal backend only
- Error handling prevents information leakage
- React Query handles stale data gracefully
</threat_model>

<verification>
- Next.js dev server starts without errors on port 3000
- Page renders correctly at http://localhost:3000
- TailwindCSS classes apply correctly
- React Query devtools accessible (if enabled)
- API client can make requests to backend
</verification>

<success_criteria>
- Next.js app runs on mobile (375px) and desktop (1280px)
- TailwindCSS is properly configured
- React Query provider wraps the application
- API client is configured for backend communication
- TypeScript compiles without errors
- App builds successfully with npm run build
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-core/04-01-SUMMARY.md`
</output>
