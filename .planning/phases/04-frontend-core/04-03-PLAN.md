---
phase: 04-frontend-core
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/components/map/venue-map.tsx
  - frontend/src/components/map/venue-marker.tsx
  - frontend/src/components/map/map-context.tsx
  - frontend/src/hooks/use-map.ts
autonomous: true
requirements:
  - MAP-01
user_setup: []

must_haves:
  truths:
    - "Map displays in the main app view"
    - "Map centers on user's search location"
    - "Venue pins appear on the map"
    - "Pins cluster when zoomed out (multiple venues near each other)"
    - "Clicking a pin opens venue popup with name"
    - "Map is interactive (pan, zoom, tilt)"
    - "Map uses MapLibre GL JS (not Google Maps)"
    - "Map style is appropriate for London (OpenMapTiles or similar)"
  artifacts:
    - path: "frontend/src/components/map/venue-map.tsx"
      provides: "Main map component with MapLibre GL JS"
      contains: "VenueMap component"
    - path: "frontend/src/components/map/venue-marker.tsx"
      provides: "Venue pin markers with clustering"
      contains: "maplibre-gl marker cluster"
    - path: "frontend/src/hooks/use-map.ts"
      provides: "Map state and control hook"
      contains: "useMap hook"
  key_links:
    - from: "frontend/src/components/map/venue-map.tsx"
      to: "use-search hook"
      via: "React Context"
      pattern: "useSearchContext"
    - from: "frontend/src/components/map/venue-marker.tsx"
      to: "Venue data"
      via: "props"
      pattern: "venues prop"
    - from: "frontend/src/components/map/venue-map.tsx"
      to: "MapLibre GL JS"
      via: "import"
      pattern: "maplibre-gl"
---

<objective>
Integrate MapLibre GL JS with dynamic venue pins and clustering for the KidSpot London frontend.

Purpose: Display an interactive map showing venue locations with clustering for density visualization, centered on the user's search location.

Output: Working map component with venue markers and clustering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/ubuntu/kidspot/technical_overview.md
@.planning/phases/04-frontend-core/04-01-PLAN.md
@.planning/phases/04-frontend-core/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create map context and hook</name>
  <files>frontend/src/components/map/map-context.tsx, frontend/src/hooks/use-map.ts</files>
  <action>Create map state management:
1. Create frontend/src/components/map/map-context.tsx:
   - MapContext with React Context
   - State: { map, setMap, selectedVenueId, setSelectedVenueId }
   - Provider component wrapping map
   - useMapContext hook for accessing map instance

2. Create frontend/src/hooks/use-map.ts:
   - useMap hook for controlling map
   - flyTo(lat, lon, zoom?) function
   - fitBounds for showing all markers
   - getMapState function
   - Hooks into MapContext

This allows other components to interact with the map.</action>
  <verify>Map context provides map instance to child components</verify>
  <done>Map context and hook created</done>
</task>

<task type="auto">
  <name>Task 2: Create venue marker with clustering</name>
  <files>frontend/src/components/map/venue-marker.tsx</files>
  <action>Create venue marker component:
1. Create frontend/src/components/map/venue-marker.tsx:
   - Props: { venues: Venue[], onVenueClick: (venue: Venue) => void }
   - Uses MapLibre GL JS marker and cluster
   - Implements supercluster for point clustering
   - Cluster markers show count
   - Individual markers show pin icon
   - Different colors for sponsor tiers (gold, silver, bronze)
   - Click handler for individual markers
   - Popup with venue name on marker click

2. Cluster configuration:
   - radius: 60
   - maxZoom: 14
   - minPoints: 2

3. Styling:
   - Gold sponsor: yellow/gold marker
   - Silver sponsor: silver/gray marker  
   - Bronze sponsor: bronze/copper marker
   - Regular: blue marker
   - Cluster: circle with count number

The clustering allows the map to handle dense venue areas.</action>
  <verify>Markers render on map, clusters appear when zoomed out</verify>
  <done>Venue marker component with clustering created</done>
</task>

<task type="auto">
  <name>Task 3: Create main venue map component</name>
  <files>frontend/src/components/map/venue-map.tsx</files>
  <action>Create main map component:
1. Create frontend/src/components/map/venue-map.tsx:
   - Props: { venues: Venue[], onVenueSelect?: (venue: Venue) => void }
   - Initializes MapLibre GL JS map
   - Configures map style (use OpenMapTiles style for London)
   - Sets initial view centered on search location (from use-search context)
   - Adds navigation controls (zoom, compass)
   - Integrates VenueMarker component
   - Cleanup on unmount

2. Map configuration:
   - Style: 'https://tiles.openmaptiles.org/data/style.json' or similar
   - AttributionControl: false (we add our own)
   - Touch controls for mobile
   - pitchWithRotate: false for mobile simplicity

3. Mobile considerations:
   - Map takes full width, fixed height
   - On mobile: 50vh height
   - On desktop: calc(100vh - header height)

The map is the main visual component showing venues.</action>
  <verify>Map renders with correct style, venues appear as markers</verify>
  <done>Venue map component created</done>
</task>

<task type="auto">
  <name>Task 4: Add map CSS and integrate into main page</name>
  <files>frontend/src/app/page.tsx, frontend/src/app/globals.css</files>
  <action>Style and integrate the map:
1. Update frontend/src/app/globals.css:
   - Add MapLibre GL CSS imports
   - Custom styles for map controls
   - Popup styles for marker popups
   - Cluster marker styles

2. Update frontend/src/app/page.tsx:
   - Import VenueMap from components
   - Use useSearch hook for location
   - Use React Query fetchVenues for venue data
   - Pass venues to VenueMap
   - Simple mobile-first layout: search on top, map below

This creates the basic map integration.</action>
  <verify>Map renders on main page, shows venues from search results</verify>
  <done>Map CSS and integration complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Map Tiles Server | Vector tile requests for map display |
| Frontend → Venue Data | Displaying fetched venue locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Venue Locations | accept | Venue locations are public info |
| T-04-03-02 | Denial of Service | Map Tile Server | accept | Multiple tile servers available |
| T-04-03-03 | Tampering | Map Markers | mitigate | Markers populated from validated API data |

**Critical Mitigations for This Plan:**
- Venue data from validated API responses
- Map tiles from reputable sources (OpenMapTiles)
- No sensitive data displayed on map
</threat_model>

<verification>
- Map displays on mobile and desktop
- Venue pins appear on the map
- Pins cluster when zoomed out
- Clicking a pin shows popup with venue name
- Map is interactive (pan, zoom)
- Map centers on search location
</verification>

<success_criteria>
- Map displays venue pins with clustering
- Map is interactive (pan, zoom, tilt)
- Map centers on user's search location
- Pins show sponsor tier colors
- Clicking pin opens popup with name
- Map works on mobile (touch controls)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-core/04-03-SUMMARY.md`
</output>
