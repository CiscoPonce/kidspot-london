---
phase: 07-improvement
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/venue/[slug]/page.tsx
  - frontend/src/app/venues-in/[borough]/page.tsx
  - frontend/src/app/venues-by/[type]/page.tsx
  - frontend/src/app/sitemap.ts
autonomous: true
requirements:
  - SEO-04
must_haves:
  truths:
    - Venue detail pages use Incremental Static Regeneration (ISR) for optimal TTFB.
    - Programmatic list pages use ISR for stability during traffic spikes.
    - Venue detail pages provide robust child-friendly JSON-LD schemas.
    - The sitemap uses Next.js route chunking to scale to an index as the dataset grows.
    - The frontend successfully type checks and builds without errors.
  artifacts:
    - path: frontend/src/app/venue/[slug]/page.tsx
      provides: ISR cache configuration and rich JSON-LD markup.
    - path: frontend/src/app/sitemap.ts
      provides: Chunked sitemap generator supporting large dynamic routes via `generateSitemaps`.
  key_links:
    - from: frontend/src/app/sitemap.ts
      to: frontend/src/app/venue/[slug]/page.tsx
      via: Indexed sitemap URLs for search engines to crawl.
---

<objective>
Implement Advanced SEO and SSR Optimization.

Purpose: As the dataset grows, serving static assets and providing structured data is crucial for search ranking and performance. We must utilize Incremental Static Regeneration (ISR) on venue pages, enhance semantic markup (JSON-LD) for child-friendliness, and prepare the sitemap for large-scale indexing.
Output: ISR-enabled Next.js pages, rich JSON-LD data blocks, and chunked sitemap configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable ISR and Enrich JSON-LD on Venue Detail Pages</name>
  <files>frontend/src/app/venue/[slug]/page.tsx</files>
  <action>
    - Add `export const revalidate = 3600;` (1 hour) to enable Incremental Static Regeneration (ISR).
    - Expand the `jsonLd` object inside `VenuePage`:
      - Add `aggregateRating` if `venue.rating` and `venue.user_ratings_total` exist.
      - Add explicit child-friendly indicator attributes via schema properties (e.g., `amenityFeature` for baby changing facilities, or generic labels marking 'Child Friendly').
    - Verify `generateMetadata` aligns exactly with Next.js 14 standards.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Venue pages cache efficiently via ISR and produce rich search results through enhanced JSON-LD.</done>
</task>

<task type="auto">
  <name>Task 2: Enable ISR and Add JSON-LD to Programmatic List Pages</name>
  <files>
    frontend/src/app/venues-in/[borough]/page.tsx,
    frontend/src/app/venues-by/[type]/page.tsx
  </files>
  <action>
    - Add `export const revalidate = 86400;` (1 day) to both list pages to enable ISR.
    - Inject a `script` tag containing `application/ld+json` representing an `ItemList` or `CollectionPage` of the venues displayed on that page. This groups these entries for search crawlers.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>List pages use ISR and output valid ItemList JSON-LD representing their contents.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Sitemap for Massive Scale</name>
  <files>frontend/src/app/sitemap.ts</files>
  <action>
    - Refactor `sitemap.ts` to support Next.js 14 sitemap chunking.
    - Implement `export async function generateSitemaps()`: fetch total slugs via `fetchAllSlugs()` and calculate chunk iterations (e.g., slices of 5000 venues per chunk).
    - Update the default exported function to accept the `id` param from Next.js (e.g., `export default async function sitemap({ id }: { id: number })`).
    - For `id === 0`, return base routes (homepage, boroughs, categories) along with the first slice of venues. Subsequent `id` values should only return the corresponding slice of venue routes.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Sitemap uses `generateSitemaps`, allowing Next.js to dynamically partition sitemaps into an index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Next.js Application Server | Search engine crawlers requesting massive sitemap lists or pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Denial of Service | `frontend/src/app/sitemap.ts` | mitigate | Chunking sitemaps via `generateSitemaps` limits memory consumed per index request, preventing memory exhaustion. |
| T-07-09-02 | Information Disclosure | `frontend/src/app/venue/[slug]/page.tsx` | accept | All SEO properties and page data are meant for public consumption and indexing. |
</threat_model>

<verification>
- Verify that `frontend/src/app/sitemap.ts` exports `generateSitemaps`.
- Verify that all three updated route pages include an `export const revalidate` directive.
- Run `npm run build` in the `frontend` directory to ensure successful deployment readiness.
</verification>

<success_criteria>
- Venue pages and programmatic lists are served dynamically yet cached via ISR.
- JSON-LD blocks on detailed and list pages enrich SEO crawlability.
- The sitemap accurately fragments and maps large quantities of URLs into an index.
</success_criteria>

<output>
After completion, create `.planning/phases/07-improvement/07-09-SUMMARY.md`
</output>