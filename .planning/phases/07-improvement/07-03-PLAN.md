---
phase: 07-improvement
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified: [backend/src/clients/db.ts, backend/src/clients/redis.ts, backend/src/server.js, backend/src/routes/search.js, backend/src/routes/sponsors.js]
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "Only one PostgreSQL pool instance is created for the app"
    - "Only one Redis client instance is created for the app"
    - "Per-route dotenv.config calls are removed"
  artifacts:
    - path: "backend/src/clients/db.ts"
      provides: "Shared PostgreSQL connection pool"
    - path: "backend/src/clients/redis.ts"
      provides: "Shared Redis client"
  key_links:
    - from: "backend/src/routes/*.js"
      to: "backend/src/clients/*.ts"
      via: "importing shared client"
---

<objective>
Refactor backend to use shared database and Redis connection pools (P0 ship-blocker).

Purpose: Prevent connection exhaustion and ensure efficient resource usage by eliminating per-route client creation.
Output: Centralized connection clients and refactored routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-02-SUMMARY.md
@prposal.md
@backend/src/server.js
@backend/src/routes/search.js
@backend/src/routes/sponsors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize Database and Redis clients</name>
  <files>backend/src/clients/db.ts, backend/src/clients/redis.ts, backend/src/server.js</files>
  <action>
    1. Create/Update backend/src/clients/db.ts: Export a single 'Pool' instance using environment variables. Handle pool errors globally.
    2. Update backend/src/clients/redis.ts: Ensure it exports a single 'Redis' instance (already started in 07-02).
    3. Update backend/src/server.js: 
       - Load 'dotenv/config' exactly once at the entrypoint.
       - Initialize shared clients and verify connectivity on startup.
  </action>
  <verify>
    <automated>grep -r "new Pool" backend/src | wc -l (should be 1)</automated>
  </verify>
  <done>Shared clients are created once; 'dotenv/config' is centralized.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor routes to use shared clients</name>
  <files>backend/src/routes/search.js, backend/src/routes/sponsors.js</files>
  <action>
    1. In search.js and sponsors.js:
       - Remove all 'new Pool()', 'new Redis()', and 'dotenv.config()' calls (per P0-9).
       - Import shared 'db' and 'redis' clients from backend/src/clients/.
       - Update database queries to use the shared pool (e.g., db.query).
  </action>
  <verify>
    <automated>npm run test-search-api && npm run test-sponsor-api</automated>
  </verify>
  <done>Routes are clean and use shared resource pools; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Database | Shared connection pool |
| API -> Redis | Shared connection pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service | Database/Redis | mitigate | Connection pooling prevents resource exhaustion (P0-9) |
</threat_model>

<verification>
1. Verify no 'new Pool' or 'new Redis' exists in routes.
2. Verify 'dotenv.config' is only in entrypoints.
3. Verify search and sponsor APIs still function.
</verification>

<success_criteria>
- Single connection pool for PG.
- Single client for Redis.
- Zero per-route client creation.
</success_criteria>

<output>
After completion, create .planning/phases/07-improvement/07-03-SUMMARY.md
</output>
