---
phase: 07-improvement
plan: 06
type: execute
wave: 1
depends_on: [07-04]
files_modified: [backend/db/migrations/004_enrich_venues.sql, backend/db/schema.sql, backend/src/types/venue.ts, backend/db/migrations/005_update_search_function.sql, backend/scripts/backfill-kid-score.ts, backend/scripts/cron-agent.js]
autonomous: true
requirements: [DATA-04, RANK-01, PIPE-01]

must_haves:
  truths:
    - "Venues table has kid_score, rating, and user_ratings_total columns"
    - "search_venues_by_radius function returns kid_score and uses it for secondary ranking"
    - "Venue interface includes the new scoring and rating fields"
    - "Backfill script successfully calculates scores for existing venues"
    - "Cron agent automatically updates kid_score during venue enrichment"
  artifacts:
    - path: "backend/db/migrations/004_enrich_venues.sql"
      provides: "Schema updates for scoring"
    - path: "backend/db/migrations/005_update_search_function.sql"
      provides: "Updated ranking function"
    - path: "backend/scripts/backfill-kid-score.ts"
      provides: "One-time scoring backfill"
  key_links:
    - from: "backend/scripts/cron-agent.js"
      to: "backend/src/scoring/kidScore.ts"
      via: "scoring calculation"
    - from: "backend/db/migrations/005_update_search_function.sql"
      to: "venues table"
      via: "query column 'kid_score'"
---

<objective>
Enrich the data model and persist the Kid Score in the database for better ranking. This move from runtime calculation to database-level persistence allows for efficient sorting, filtering, and more sophisticated ranking logic while reducing API latency.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/db/schema.sql
@backend/db/migrations/001_create_venues_table.sql
@backend/src/types/venue.ts
@backend/src/scoring/kidScore.ts
@backend/scripts/cron-agent.js
</context>

<tasks>

<task type="auto">
  <name>Update Data Model: Schema &amp; Types</name>
  <files>backend/db/migrations/004_enrich_venues.sql, backend/db/schema.sql, backend/src/types/venue.ts</files>
  <action>
    - Create `backend/db/migrations/004_enrich_venues.sql` to add columns: `kid_score` (NUMERIC), `rating` (NUMERIC), `user_ratings_total` (INTEGER), and `enriched_at` (TIMESTAMPTZ) to the `venues` table.
    - Update `backend/db/schema.sql` to include these columns in the base table definition for new environments.
    - Update the `Venue` interface in `backend/src/types/venue.ts` to include these new fields, ensuring they are optional to maintain compatibility with partial data.
    - Ensure `kid_score` has a default of 0.
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "\d venues" | grep -E "kid_score|rating|user_ratings_total|enriched_at"</automated>
  </verify>
  <done>Database schema and TypeScript types are aligned with the enriched data model.</done>
</task>

<task type="auto">
  <name>Update Search Logic: Ranking Function</name>
  <files>backend/db/migrations/005_update_search_function.sql</files>
  <action>
    - Create `backend/db/migrations/005_update_search_function.sql` to redefine the `search_venues_by_radius` PostGIS function.
    - Add `kid_score` to the `RETURNS TABLE` definition.
    - Include `v.kid_score` in the `SELECT` statement.
    - Update the `ORDER BY` clause to prioritize `kid_score` DESC as the secondary ranking factor (immediately after sponsor tier/priority and before distance).
    - Ensure results within the same sponsorship tier are sorted by `kid_score` before distance.
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "\df search_venues_by_radius"</automated>
  </verify>
  <done>Search function now returns the Kid Score and uses it to drive venue ranking.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement Backfill &amp; Continuous Enrichment</name>
  <files>backend/scripts/backfill-kid-score.ts, backend/scripts/cron-agent.js</files>
  <behavior>
    - Backfill script should process all existing venues, use their current name/types/rating to calculate a score, and update the record.
    - Cron agent should fetch `rating` and `user_ratings_total` from Google Places API during its regular update cycle.
    - Cron agent should call `calculateKidScore` and persist the result along with the new rating data.
  </behavior>
  <action>
    - Create `backend/scripts/backfill-kid-score.ts` using the `calculateKidScore` logic from `backend/src/scoring/kidScore.ts`.
    - Update `backend/scripts/cron-agent.js`:
      - Update the `fields` parameter in `getVenueDetails` to include `rating,user_ratings_total`.
      - Modify `updateVenue` to calculate the `kid_score` and update the new columns in the `venues` table.
      - Ensure the cron agent marks `enriched_at` during the update.
    - Use `tsx scripts/backfill-kid-score.ts` to run the backfill once the script is ready.
  </action>
  <verify>
    <automated>npm run cron -- --dry-run &amp;&amp; tsx backend/scripts/backfill-kid-score.ts --limit 5</automated>
  </verify>
  <done>All existing venues have been scored, and the continuous discovery pipeline now maintains these scores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Client | Enriched data (scores, ratings) is exposed to the frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | venues.kid_score | mitigate | Use database-level constraints or read-only API access to ensure scores are only modified by backend processes |
| T-07-06-02 | Information Disclosure | venues.rating | mitigate | Only expose necessary rating fields to public API; keep internal metadata like `enriched_at` for system use |
</threat_model>

<verification>
1. Run migrations 004 and 005.
2. Run `tsx backend/scripts/backfill-kid-score.ts` and verify `kid_score > 0` for known high-quality venues (e.g. parks, softplays).
3. Execute a search via API and confirm `kid_score` is present in the response and influences ranking.
4. Run `npm run cron` and verify that updated venues have an `enriched_at` timestamp and a calculated score.
</verification>

<success_criteria>
- `venues` table contains `kid_score`, `rating`, `user_ratings_total`, and `enriched_at` columns.
- `calculateKidScore` logic is successfully applied to all existing venues via the backfill script.
- The `search_venues_by_radius` function returns `kid_score` and uses it for ranking.
- Continuous enrichment via the cron agent persists the Kid Score for all newly discovered or updated venues.
</success_criteria>

<output>
After completion, create `.planning/phases/07-improvement/07-06-SUMMARY.md`
</output>
