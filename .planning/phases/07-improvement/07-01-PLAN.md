---
phase: 07-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/worker.js, docker-compose.yml, backend/db/migrations/003_composite_source_unique.sql, backend/src/routes/search.js, backend/src/server.js, backend/src/middleware/admin.ts]
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, SEC-01]

must_haves:
  truths:
    - "Worker service runs without crash-looping in Docker"
    - "Brave fallback venues do not map to user location"
    - "Google Places API calls return populated reviews"
    - "Admin endpoints are timing-safe and secure"
    - "/ready endpoint verifies database and redis health"
  artifacts:
    - path: "backend/src/worker.js"
      provides: "BullMQ worker entrypoint"
    - path: "backend/db/migrations/003_composite_source_unique.sql"
      provides: "Composite unique constraint for venues"
    - path: "backend/src/middleware/admin.ts"
      provides: "Timing-safe admin authentication"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/src/worker.js"
      via: "worker service command"
    - from: "backend/src/server.js"
      to: "Postgres/Redis"
      via: "/ready health check"
---

<objective>
Stabilize core infrastructure and fix critical search logic bugs (P0 ship-blockers).

Purpose: Stop the worker crash-loop, prevent data corruption on fallback results, and secure the admin interface.
Output: Working worker service, corrected search mapping, and timing-safe admin auth.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-RESEARCH.md
@prposal.md
@backend/src/server.js
@backend/src/routes/search.js
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix worker service stability and DB constraints</name>
  <files>backend/src/worker.js, docker-compose.yml, backend/db/migrations/003_composite_source_unique.sql</files>
  <action>
    1. Create backend/src/worker.js as a minimal BullMQ worker entrypoint that connects to Redis and stays alive (per P0-1).
    2. Update docker-compose.yml: Ensure the 'worker' service uses the correct build context and 'command: node src/worker.js' (per P0-1).
    3. Create backend/db/migrations/003_composite_source_unique.sql to change source_id UNIQUE constraint to UNIQUE (source, source_id) (per P0-3).
    4. Apply migration to the database using existing migration scripts or direct psql.
  </action>
  <verify>
    <automated>docker compose up -d worker && sleep 5 && docker compose ps worker | grep "Up"</automated>
  </verify>
  <done>Worker service is running without restarts; composite unique index exists in Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Fix search result bugs (Brave & Google)</name>
  <files>backend/src/routes/search.js</files>
  <action>
    1. Fix Brave fallback coordinate bug: In backend/src/routes/search.js, stop assigning the searcher's lat/lon to fallback results. Either geocode the address or flag as non-mappable (per P0-2).
    2. Fix Google Places field mask: In fetchGooglePlaceDetails, fix the 'review_types' typo and ensure valid fields are requested (per P0-4). Note: If possible, pin to v1 as suggested in RESEARCH.md.
  </action>
  <verify>
    <automated>npm run test-search-api (verify fallback results have unique coordinates or are handled correctly)</automated>
  </verify>
  <done>Brave fallback results no longer cluster on search location; Google reviews are correctly fetched.</done>
</task>

<task type="auto">
  <name>Task 3: Security hardening and Health monitoring</name>
  <files>backend/src/server.js, backend/src/middleware/admin.ts</files>
  <action>
    1. Create backend/src/middleware/admin.ts: Implement timing-safe comparison using crypto.timingSafeEqual for admin auth (per P0-5).
    2. Update backend/src/server.js: 
       - Add a deep '/ready' endpoint that checks Postgres (SELECT 1) and Redis (PING) health (per P0-8).
       - Configure explicit Content Security Policy (CSP) in Helmet (per P0-5).
       - Lock CORS to production origin if available in env.
  </action>
  <verify>
    <automated>curl -f http://localhost:4000/ready</automated>
  </verify>
  <done>/ready returns 200 when healthy; admin auth uses timing-safe compare; CSP headers are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Public search and detail requests |
| Admin -> API | Protected ingestion and management requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | search.js (Brave) | mitigate | Fix coordinate assignment to prevent data corruption |
| T-07-02 | Information Disclosure | server.js | mitigate | Timing-safe admin auth (P0-5) |
| T-07-03 | Denial of Service | /ready | mitigate | Deep health check ensures early failure/restart (P0-8) |
</threat_model>

<verification>
1. Verify all docker services are 'Up'.
2. Verify /ready returns 200.
3. Verify search fallback results do not share searcher coordinates.
</verification>

<success_criteria>
- Worker service is stable.
- Search logic fixes deployed.
- Admin security hardened.
</success_criteria>

<output>
After completion, create .planning/phases/07-improvement/07-01-SUMMARY.md
</output>
