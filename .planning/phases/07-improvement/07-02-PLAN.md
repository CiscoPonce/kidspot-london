---
phase: 07-improvement
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [backend/package.json, backend/src/clients/redis.ts, backend/src/middleware/rateLimit.ts, backend/src/config/logger.ts, backend/src/server.js, backend/src/routes/search.js]
autonomous: true
requirements: [PERF-02, SEC-01]

must_haves:
  truths:
    - "Rate limiting uses Redis for multi-instance safety"
    - "Logging is structured (JSON) and includes Request IDs"
    - "Console logs are replaced with structured pino logs"
  artifacts:
    - path: "backend/src/config/logger.ts"
      provides: "Pino logger configuration"
    - path: "backend/src/middleware/rateLimit.ts"
      provides: "Redis-backed rate limiting"
  key_links:
    - from: "backend/src/server.js"
      to: "backend/src/config/logger.ts"
      via: "app-wide logging middleware"
---

<objective>
Implement production-grade logging and cluster-safe rate limiting (P0 ship-blockers).

Purpose: Improve observability with structured logging and ensure rate limits are enforced consistently across multiple instances.
Output: Structured JSON logs and Redis-backed rate limiting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-01-SUMMARY.md
@prposal.md
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup structured logging with Pino</name>
  <files>backend/package.json, backend/src/config/logger.ts, backend/src/server.js</files>
  <action>
    1. Install pino and pino-http in backend/package.json.
    2. Create backend/src/config/logger.ts to configure pino with structured JSON output (per P0-7).
    3. Update backend/src/server.js to use pino-http middleware and include a unique Request ID (req.id) in all logs.
    4. Start replacing console.log calls in server.js and routes with logger.info/error.
  </action>
  <verify>
    <automated>npm run test (ensure logs are JSON formatted and include req.id)</automated>
  </verify>
  <done>Logs are output as structured JSON; Request IDs are present in every log line.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redis-backed rate limiting</name>
  <files>backend/package.json, backend/src/clients/redis.ts, backend/src/middleware/rateLimit.ts, backend/src/server.js, backend/src/routes/search.js</files>
  <action>
    1. Install rate-limit-redis in backend/package.json.
    2. Create/Update backend/src/clients/redis.ts to provide a shared Redis instance.
    3. Create backend/src/middleware/rateLimit.ts using rate-limit-redis to enforce a 60 req/min limit per IP (per P0-6).
    4. Remove in-memory 'lastBraveSearchTime' from search.js and replace it with a Redis-based lock or rate limit (per P0-6).
    5. Wire the global rate limiter into server.js.
  </action>
  <verify>
    <automated>npm run test-security (verify rate limiting returns 429 after 60 reqs)</automated>
  </verify>
  <done>Rate limiting is multi-instance safe via Redis; 1 r/s Brave quota is respected across workers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet -> API | Rate limiting boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service | API | mitigate | Redis-backed rate limiting (P0-6) |
| T-07-05 | Information Disclosure | Logs | mitigate | Structured logging with Request IDs for better audit (P0-7) |
</threat_model>

<verification>
1. Verify logs are JSON.
2. Verify 429 response after exceeding rate limit.
</verification>

<success_criteria>
- Structured logging active.
- Redis-backed rate limiting active.
</success_criteria>

<output>
After completion, create .planning/phases/07-improvement/07-02-SUMMARY.md
</output>
