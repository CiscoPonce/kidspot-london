---
phase: 07-improvement
plan: 07
type: execute
wave: 3
depends_on: ["07-04", "07-06"]
files_modified: ["backend/package.json", "backend/src/schemas/searchSchema.ts", "backend/src/controllers/searchController.ts"]
autonomous: true
requirements: ["SEC-01", "ARCH-01"]

must_haves:
  truths:
    - "Search API rejects requests with invalid parameters (e.g., missing lat/lon)"
    - "Valid search requests are correctly typed and processed"
  artifacts:
    - path: "backend/src/schemas/searchSchema.ts"
      provides: "Zod schemas for search queries"
      exports: ["searchQuerySchema"]
  key_links:
    - from: "backend/src/controllers/searchController.ts"
      to: "backend/src/schemas/searchSchema.ts"
      via: "zod parse/validate"
      pattern: "searchQuerySchema.parse"
---

<objective>
Implement Search Optimization and Zod Validation for API endpoints.

Purpose: To ensure robust input validation (SEC-01) for search parameters, preventing malformed requests from hitting the database or external APIs, and establishing a unified schema validation standard.
Output: Zod schemas and integrated validation in the search controller.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-improvement/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zod</name>
  <files>backend/package.json</files>
  <action>Install the `zod` package in the backend directory. Run `npm install zod`.</action>
  <verify>
    <automated>cd backend && npm ls zod</automated>
  </verify>
  <done>Zod is listed in package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zod schemas for search</name>
  <files>backend/src/schemas/searchSchema.ts</files>
  <behavior>
    - Test 1: Validates valid search parameters (lat, lon, radius) correctly.
    - Test 2: Rejects missing required parameters (lat, lon).
    - Test 3: Rejects invalid types or out-of-bounds coordinates.
  </behavior>
  <action>Create a new file `backend/src/schemas/searchSchema.ts`. Define and export `searchQuerySchema` using Zod to validate `lat` (number, valid coordinate), `lon` (number, valid coordinate), `radius_miles` (number, positive), `type` (optional string), and `borough` (optional string). Coerce query string values to numbers where appropriate.</action>
  <verify>
    <automated>ls backend/src/schemas/searchSchema.ts</automated>
  </verify>
  <done>Schema file exists and exports searchQuerySchema</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Zod validation in controller</name>
  <files>backend/src/controllers/searchController.ts</files>
  <action>Update `backend/src/controllers/searchController.ts` to use `searchQuerySchema.safeParse()` on the incoming request query parameters. If validation fails, return a 400 Bad Request with the validation errors. Ensure the validated data is used for subsequent logic.</action>
  <verify>
    <automated>cd backend && npm test -- --run</automated>
  </verify>
  <done>Controller uses Zod for validation and tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client API -> Backend | Untrusted search query parameters enter the backend here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | searchController.ts | mitigate | Validate all incoming query parameters (lat, lon, radius) using Zod schema before processing. |
</threat_model>

<verification>
Ensure all tasks run successfully and tests pass.
</verification>

<success_criteria>
Search API endpoints enforce validation via Zod, rejecting invalid parameters with a 400 error.
</success_criteria>

<output>
After completion, create `.planning/phases/07-improvement/07-07-SUMMARY.md`
</output>