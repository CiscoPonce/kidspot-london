---
phase: 07-improvement
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified: [backend/tsconfig.json, backend/src/types/venue.ts, backend/src/services/venueService.ts, backend/src/controllers/searchController.ts, backend/src/routes/search.js]
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "Backend uses TypeScript with 'strict: true'"
    - "Search logic is moved from routes to services"
    - "Unified Venue type is shared across backend and frontend"
  artifacts:
    - path: "backend/tsconfig.json"
      provides: "TypeScript configuration"
    - path: "backend/src/services/venueService.ts"
      provides: "Business logic for venues"
  key_links:
    - from: "backend/src/controllers/searchController.ts"
      to: "backend/src/services/venueService.ts"
      via: "method calls"
---

<objective>
Refactor backend to a layered architecture and begin TypeScript migration (P1 Quality Foundation).

Purpose: Improve code maintainability, testability, and type safety by separating concerns and adopting TypeScript.
Output: TypeScript configuration, unified types, and first layered service/controller.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-03-SUMMARY.md
@prposal.md
@backend/src/routes/search.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup TypeScript and Unified Types</name>
  <files>backend/tsconfig.json, backend/package.json, backend/src/types/venue.ts</files>
  <action>
    1. Initialize backend/tsconfig.json with 'strict: true', 'allowJs: true', and 'outDir: ./dist' (per P1-2).
    2. Add TypeScript dev dependencies to backend/package.json.
    3. Create backend/src/types/venue.ts: Define a unified 'Venue' interface based on the database schema and search API requirements. This should be used by both backend and eventually frontend (per P1-2).
  </action>
  <verify>
    <automated>npx tsc --noEmit (verify no configuration errors)</automated>
  </verify>
  <done>TypeScript is configured; unified Venue interface is defined.</done>
</task>

<task type="auto">
  <name>Task 2: Layered refactor of Search logic</name>
  <files>backend/src/services/venueService.ts, backend/src/controllers/searchController.ts, backend/src/routes/search.js</files>
  <action>
    1. Create backend/src/services/venueService.ts: Move spatial search and Brave fallback logic here. Use shared db/redis clients (per P1-1).
    2. Create backend/src/controllers/searchController.ts: Handle request parsing, validation (preparatory for zod), and call venueService (per P1-1).
    3. Update backend/src/routes/search.js: Refactor to a thin router that calls searchController methods.
  </action>
  <verify>
    <automated>npm run test-search-api</automated>
  </verify>
  <done>Search logic is refactored into service/controller layers; routing is thin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> Service | Business logic boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | Type definitions | mitigate | Strong typing reduces 'any' usage and potential leakage |
</threat_model>

<verification>
1. Verify 'backend/dist' can be compiled.
2. Verify search API functionality.
</verification>

<success_criteria>
- TypeScript initialized.
- Unified types created.
- Search logic separated into service/controller.
</success_criteria>

<output>
After completion, create .planning/phases/07-improvement/07-04-SUMMARY.md
</output>
