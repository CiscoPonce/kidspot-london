---
phase: 07-improvement
plan: 05
type: execute
wave: 1
depends_on: ["07-04-SUMMARY.md"]
files_modified: [
  "backend/package.json",
  "backend/src/scoring/kidScore.ts",
  "backend/src/scoring/kidScore.test.ts",
  "backend/src/tests/integration/search.test.ts",
  "backend/vitest.config.ts",
  ".github/workflows/ci.yml"
]
autonomous: true
requirements: ["TEST-01", "RANK-01", "PIPE-01"]

must_haves:
  truths:
    - "Backend tests run using Vitest"
    - "calculateKidScore logic correctly applies weights and disqualifications"
    - "Integration tests verify search API responses"
    - "CI workflow triggers on push/PR to main"
  artifacts:
    - path: "backend/src/scoring/kidScore.ts"
      provides: "Ranking engine logic"
    - path: "backend/vitest.config.ts"
      provides: "Vitest configuration"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline definition"
  key_links:
    - from: "backend/src/scoring/kidScore.test.ts"
      to: "backend/src/scoring/kidScore.ts"
      via: "Unit tests"
    - from: "backend/src/tests/integration/search.test.ts"
      to: "backend/src/server.ts"
      via: "Supertest API calls"
---

<objective>
Establish a quality baseline for the KidSpot backend with automated testing and a basic CI workflow. Implement the core ranking logic to enable future data enrichment.

Purpose: Ensure long-term stability and prevent regressions as the platform scales.
Output: Working test suite (Vitest), scoring engine implementation, and GitHub Actions CI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-improvement/07-RESEARCH.md
@.planning/phases/07-improvement/07-04-SUMMARY.md
@backend/src/server.ts
@backend/src/types/venue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Vitest and test infrastructure</name>
  <files>backend/package.json, backend/vitest.config.ts</files>
  <action>
    - Install vitest, @vitest/coverage-v8, supertest, and @types/supertest as devDependencies in backend/.
    - Remove jest and related dependencies/scripts from backend/package.json to standardize on Vitest.
    - Create backend/vitest.config.ts with support for TypeScript and ESM.
    - Update backend/package.json scripts:
      - "test": "vitest run"
      - "test:watch": "vitest"
      - "test:coverage": "vitest run --coverage"
  </action>
  <verify>
    <automated>cd backend && npm test -- --help</automated>
  </verify>
  <done>Vitest is installed and configured as the primary test runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoring engine and unit tests</name>
  <files>backend/src/scoring/kidScore.ts, backend/src/scoring/kidScore.test.ts</files>
  <behavior>
    - Test 1: Disqualification - Returns 0 if types include 'bar', 'casino', etc.
    - Test 2: Base Weight - Returns correct score for 'softplay' (10), 'park' (8), etc.
    - Test 3: Keyword Boost - Increases score for 'birthday', 'party', 'play' in name.
    - Test 4: Rating Adjustment - Adds (rating/5)*2 if ratings total > 5.
    - Test 5: Capping - Score never exceeds 20.
  </behavior>
  <action>
    - Create backend/src/scoring/kidScore.ts implementing calculateKidScore based on logic in 07-RESEARCH.md.
    - Ensure it handles the current Venue interface properties correctly.
    - Implement comprehensive unit tests in backend/src/scoring/kidScore.test.ts using Vitest.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/scoring/kidScore.test.ts</automated>
  </verify>
  <done>Scoring logic is implemented and 100% verified by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Search API integration tests</name>
  <files>backend/src/tests/integration/search.test.ts</files>
  <action>
    - Create integration tests for the search API using supertest and the app instance from src/server.ts.
    - Mock the database and redis clients to avoid external dependencies in CI.
    - Tests should cover:
      - GET /api/search/venues with valid lat/lon.
      - GET /api/search/venues with valid borough.
      - GET /api/search/venues with missing parameters (400).
      - Verify response structure matches SearchResponse interface.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/tests/integration/search.test.ts</automated>
  </verify>
  <done>Core API endpoints are verified via integration tests with mocked I/O.</done>
</task>

<task type="auto">
  <name>Task 4: Configure CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    - Create .github/workflows/ci.yml to run on push to main or pull requests.
    - Steps should include:
      - Checkout code.
      - Setup Node.js (v20 as per current engine, will upgrade to 22 in 07-11).
      - Install dependencies for both backend and frontend.
      - Run linting (backend and frontend).
      - Run typecheck (backend and frontend).
      - Run backend tests (npm test).
    - Use caching for npm dependencies to speed up runs.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml</automated>
  </verify>
  <done>GitHub Actions CI workflow is defined and covers quality gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Environment | Secrets and environment variables used during tests must be protected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | CI Pipeline | mitigate | Use branch protection and signed commits to prevent unauthorized workflow changes. |
| T-07-05-02 | Information Disclosure | Test Logs | mitigate | Ensure no secrets are logged in CI output; use mock data for tests. |
</threat_model>

<verification>
1. Run all tests locally: `cd backend && npm test`
2. Run linting: `cd backend && npm run lint` and `cd frontend && npm run lint`
3. Run typecheck: `cd backend && npm run typecheck` and `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Vitest is the backend test runner.
- `calculateKidScore` unit tests pass.
- Search API integration tests pass (with mocks).
- GitHub Actions workflow file exists and is valid.
</success_criteria>

<output>
After completion, create `.planning/phases/07-improvement/07-05-SUMMARY.md`
</output>
