---
phase: 22-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/googlePlacesService.ts
  - backend/scripts/discovery/sources/google-places-discovery.ts
  - backend/scripts/discovery/sources/postcodesio-geocoding.ts
  - backend/scripts/discovery/chain-expansion.ts
  - backend/scripts/discovery/data-max-runner.ts
autonomous: true
requirements:
  - 22-D1
  - 22-D2
  - 22-D3
  - 22-D4
user_setup:
  - service: Google Places API
    why: "Google Places discovery sweep and chain expansion"
    env_vars:
      - name: GOOGLE_PLACES_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials"
    dashboard_config: []
  - service: postcodes.io
    why: "Batch reverse geocoding for venues missing lat/lon"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Google Places discovery sweep can find new venues not yet in the database"
    - "Chain expansion searches use Google Places Text Search API as primary method"
    - "Postcodes.io batch script enriches venues missing lat/lon or postcode"
    - "All four data sweeps can be triggered from a single orchestrator script"
  artifacts:
    - path: "backend/src/services/googlePlacesService.ts"
      provides: "searchByText() method for Google Places Text Search returning multiple results"
      contains: "textSearch"
    - path: "backend/scripts/discovery/sources/google-places-discovery.ts"
      provides: "Discovers new venues by borough/category via Google Places Text Search"
      exports:
        - "discoverVenuesViaGooglePlaces"
    - path: "backend/scripts/discovery/sources/postcodesio-geocoding.ts"
      provides: "Batch reverse geocoding for venues missing lat/lon"
      exports:
        - "geocodeViaPostcodesIo"
    - path: "backend/scripts/discovery/chain-expansion.ts"
      provides: "Chain expansion using Google Places Text Search as primary"
      contains: "googlePlacesService"
    - path: "backend/scripts/discovery/data-max-runner.ts"
      provides: "Orchestrator that runs all four sweeps in sequence with logging"
      exports:
        - "runDataMaxSweeps"
  key_links:
    - from: "google-places-discovery.ts"
      to: "googlePlacesService"
      via: "googlePlacesService.textSearch()"
      pattern: "googlePlacesService\\.textSearch"
    - from: "postcodesio-geocoding.ts"
      to: "fetch('https://api.postcodes.io/')"
      via: "HTTP POST"
      pattern: "postcodes\\.io"
    - from: "data-max-runner.ts"
      to: "google-places-discovery.ts"
      via: "dynamic import"
      pattern: "discoverVenuesViaGooglePlaces"
    - from: "data-max-runner.ts"
      to: "postcodesio-geocoding.ts"
      via: "dynamic import"
      pattern: "geocodeViaPostcodesIo"
    - from: "data-max-runner.ts"
      to: "chain-expansion.ts"
      via: "dynamic import"
      pattern: "discoverChains"
---

<objective>
Create the Data Max enrichment scripts: Google Places discovery of new venues, postcodes.io batch geocoding, chain expansion via Google Places, and a unified orchestrator to run all four sweeps (including existing Brave/Street View image enrichment).

Purpose: Fill remaining data gaps by discovering new venues, geocoding missing locations, expanding chain coverage, and backfilling images — all runnable as a single sweep or individually.
Output: Five files (1 modify, 4 new) following existing enrichment script patterns.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md

@/home/ubuntu/.planning/phases/22-launch-readiness/22-RESEARCH.md
@/home/ubuntu/.planning/phases/22-launch-readiness/22-PATTERNS.md
</execution_context>

<context>
@/home/ubuntu/kidspot/backend/src/services/googlePlacesService.ts
@/home/ubuntu/kidspot/backend/scripts/discovery/sources/google-places-enrichment.ts
@/home/ubuntu/kidspot/backend/scripts/discovery/chain-expansion.ts
@/home/ubuntu/kidspot/backend/scripts/discovery/run-discovery.ts
@/home/ubuntu/kidspot/.planning/phases/22-launch-readiness/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add searchByText() to GooglePlacesService + create discovery script</name>
  <files>
    backend/src/services/googlePlacesService.ts
    backend/scripts/discovery/sources/google-places-discovery.ts
  </files>

  <read_first>
    @/home/ubuntu/kidspot/backend/src/services/googlePlacesService.ts
    @/home/ubuntu/kidspot/backend/scripts/discovery/sources/google-places-enrichment.ts
  </read_first>

  <action>
    **Part A — Add `searchByText()` to GooglePlacesService (`backend/src/services/googlePlacesService.ts`):**

    Add a new public method `textSearch(query: string, opts?: { type?: string; locationBias?: { lat: number; lon: number }; radius?: number; maxResults?: number })` that:
    - Calls POST `https://places.googleapis.com/v1/places:searchText` with `X-Goog-FieldMask` including: `places.id,places.displayName,places.websiteUri,places.nationalPhoneNumber,places.formattedAddress,places.location,places.photos,places.businessStatus,places.types`
    - Returns an array of results (not just the first match) up to `maxResults` (default 10)
    - Follows the same rate-limit/error handling pattern as the existing `findPlace()` method (lines 46-55 of the existing file)
    - Returns `GooglePlaceMatch[]` where `GooglePlaceMatch` has fields: `placeId`, `name`, `website`, `phone`, `address`, `lat`, `lon`, `photos`, `businessStatus`, `types`
    - Add the new interface `GooglePlaceTextSearchResult` above the existing `GooglePlaceMatch`

    Per D-12 (concurrent independent sweeps), this method enables both the discovery sweep (D1) and chain expansion (D2) without relying on Apify.

    **Part B — Create discovery script (`backend/scripts/discovery/sources/google-places-discovery.ts`):**

    Follow the exact pattern from `google-places-enrichment.ts`:
    - Export `GooglePlacesDiscoveryResult` interface with `{ discovered, skipped, failed, totalProcessed }`
    - Export async function `discoverVenuesViaGooglePlaces(batchSize: number = 50)`:
      1. Query `venues` to identify which boroughs/areas have lowest venue counts (to target discovery)
      2. For each under-represented area, call `googlePlacesService.textSearch()` with relevant category keywords (soft play, leisure centre, community hall, museum, library)
      3. Use `ON CONFLICT (source, source_id) DO UPDATE` pattern for idempotent inserts (same as chain-expansion.ts line 137)
      4. Rate limit via `await new Promise(resolve => setTimeout(resolve, 500))` between calls
      5. Wrap each venue in try/catch with incrementing `failed` counter
      6. Wrap entire pipeline in try/catch with re-throw
    - CLI entry point with `isMainModule` check + `process.exit(0/1)` (same pattern as google-places-enrichment.ts lines 118-125)
    - Import from `'../../../src/services/googlePlacesService.js'` and `'../../../src/clients/db.js'`
  </action>

  <acceptance_criteria>
    - `googlePlacesService.ts` exports a new `textSearch()` method that returns multiple results for a text query
    - `google-places-discovery.ts` exists and follows the exact enrichment script pattern (Result interface, exports async function, ISE pattern, CLI entry)
    - Running `npx tsx backend/scripts/discovery/sources/google-places-discovery.ts --help` or with args does not throw import errors
    - `cd backend && npx tsc --noEmit` passes without type errors
  </acceptance_criteria>

  <verify>
    <automated>cd /home/ubuntu/kidspot/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    GooglePlacesService has `textSearch()` method returning `GooglePlaceTextSearchResult[]`. Discovery script exists with proper exports and CLI entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create postcodes.io batch geocoding script</name>
  <files>backend/scripts/discovery/sources/postcodesio-geocoding.ts</files>

  <read_first>
    @/home/ubuntu/kidspot/backend/scripts/discovery/sources/google-places-enrichment.ts
  </read_first>

  <action>
    Create `backend/scripts/discovery/sources/postcodesio-geocoding.ts` following the exact enrichment script pattern:

    Export `PostcodesioGeocodingResult` interface with `{ enriched, skipped, failed, totalProcessed }`.

    Export async function `geocodeViaPostcodesIo(batchSize: number = 100)`:
    1. Query venues that have a postcode but are missing `lat` or `lon`:
       ```sql
       SELECT id, postcode FROM venues
       WHERE is_active = TRUE
         AND venue_scope = 'core'
         AND postcode IS NOT NULL AND postcode != ''
         AND (lat IS NULL OR lon IS NULL)
       ORDER BY id ASC
       LIMIT $1
       ```
    2. For each venue: POST to `https://api.postcodes.io/postcodes/{postcode}` (free, no key)
    3. On success: UPDATE venues SET `lat`, `lon` using COALESCE pattern
    4. On failure: log and increment `failed`, do NOT throw
    5. Rate limit: `await new Promise(resolve => setTimeout(resolve, 200))` (postcodes.io is fast)
    6. Wrap entire pipeline in try/catch, re-throw for BullMQ retry

    Also query venues that have lat/lon but missing `postcode` (reverse geocoding):
    - Use `POST https://api.postcodes.io/postcodes` with `{ geolocations: [{ latitude, longitude, limit: 1 }] }`
    - Batch into groups of 100 (postcodes.io bulk limit)
    - Update `postcode` field with COALESCE/NULLIF pattern

    CLI entry point with `isMainModule` check.

    Import pattern:
    ```typescript
    import { db } from '../../../src/clients/db.js';
    import { logger } from '../../../src/config/logger.js';
    ```
  </action>

  <acceptance_criteria>
    - Script exists at `backend/scripts/discovery/sources/postcodesio-geocoding.ts`
    - Exports `geocodeViaPostcodesIo(batchSize)` function and `PostcodesioGeocodingResult` interface
    - Handles forward (postcode→lat/lon) and reverse (lat/lon→postcode) geocoding
    - CLI entry point works with `npx tsx`
    - `cd backend && npx tsc --noEmit` passes
  </acceptance_criteria>

  <verify>
    <automated>cd /home/ubuntu/kidspot/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    Postcodes.io batch geocoding script exists with forward and reverse geocoding, proper error handling, and CLI entry point.
  </done>
</task>

<task type="auto">
  <name>Task 3: Modify chain-expansion.ts for Google Places + create data-max-runner orchestrator</name>
  <files>
    backend/scripts/discovery/chain-expansion.ts
    backend/scripts/discovery/data-max-runner.ts
  </files>

  <read_first>
    @/home/ubuntu/kidspot/backend/scripts/discovery/chain-expansion.ts
    @/home/ubuntu/kidspot/backend/scripts/discovery/run-discovery.ts
  </read_first>

  <action>
    **Part A — Modify chain-expansion.ts (`backend/scripts/discovery/chain-expansion.ts`):**

    Per D-12, switch primary search from Apify to Google Places Text Search API:
    1. Add import: `import { googlePlacesService } from '../../src/services/googlePlacesService.js';`
    2. In the `discoverChains` function, before the Apify search block, add a Google Places search:
       ```typescript
       // Primary: Google Places Text Search
       const googleResults = await googlePlacesService.textSearch(searchString, {
         maxResults: 10,
         locationBias: { lat: 51.5074, lon: -0.1278 },
         radius: 50000 // 50km across London
       });
       if (googleResults.length > 0) {
         results = googleResults;
       }
       ```
    3. Keep existing Apify code as fallback (when `googleResults` is empty or no API key)
    4. The result mapping must handle both `GooglePlaceTextSearchResult` (from Google) and the existing Apify result shape — normalize to a common shape before insertion
    5. Keep the `source` field as `'google'` for both (already the value used, matching line 149 of existing code)

    **Part B — Create data-max-runner orchestrator (`backend/scripts/discovery/data-max-runner.ts`):**

    Follow the pattern from `run-discovery.ts`:
    - Export async function `runDataMaxSweeps()` that:
      1. Logs start with `=== KidSpot London - Data Max Sweep ===`
      2. Imports and runs each sweep sequentially:
         - `discoverVenuesViaGooglePlaces()` from `./sources/google-places-discovery.js`
         - `discoverChains()` from `./chain-expansion.js` (with dryRun=false)
         - `geocodeViaPostcodesIo()` from `./sources/postcodesio-geocoding.js`
         - Logs instruction to trigger existing image enrichment jobs: `enrich-brave-images` and `enrich-streetview` (these already exist as BullMQ jobs in worker.ts — D4 is satisfied by triggering them)
      3. Logs duration and results for each sweep
      4. Wraps each sweep in try/catch so one failure doesn't stop the others
    - CLI entry point with `isMainModule` check + `process.exit(0/1)`
  </action>

  <acceptance_criteria>
    - `chain-expansion.ts` uses `googlePlacesService.textSearch()` as primary search method, falls back to Apify
    - `data-max-runner.ts` exists and imports all four sweep functions
    - Running `npx tsx backend/scripts/discovery/data-max-runner.ts --dry-run` logs each step without errors
    - `cd backend && npx tsc --noEmit` passes
  </acceptance_criteria>

  <verify>
    <automated>cd /home/ubuntu/kidspot/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    Chain expansion uses Google Places as primary. Data-max-runner orchestrates all four sweeps with individual error isolation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → Google Places API | Outbound API call with API key in header |
| backend → postcodes.io | Outbound HTTP request to free geocoding service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | googlePlacesService.ts | mitigate | API key sent via `X-Goog-Api-Key` header, not URL parameter. Never logged. |
| T-22-02 | Spoofing | chain-expansion.ts | accept | Google Places results are not authoritative identity — venues use `ON CONFLICT (source, source_id)` to prevent duplicate creation. |
| T-22-03 | Denial of Service | google-places-discovery.ts | mitigate | 500ms rate limit between API calls. Timeout on fetch calls. |
| T-22-SC | Tampering | npm installs | mitigate | No new packages in this phase. All imports from existing project modules. |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit` passes
- Each script can be invoked standalone via `npx tsx <script>` without errors
</verification>

<success_criteria>
- GooglePlacesService has `textSearch()` returning multiple results
- Discovery, geocoding, chain expansion, and orchestrator scripts exist
- All TypeScript compiles without errors
- Scripts follow existing enrichment patterns (Result interface, function, CLI entry)
</success_criteria>
